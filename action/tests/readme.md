# action/tests

Shell-script tests for the scripts in `action/`.

| File | Responsibility |
|------|----------------|
| `gallery_test.sh` | Reproducer/regression tests for `action/gallery`'s HTML generation |
