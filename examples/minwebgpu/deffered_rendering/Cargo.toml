[package]
name = "minwebgpu_deffered_rendering"
version = "0.1.0"
edition = "2021"
authors = [
  "Kostiantyn Mysnyk <wandalen@obox.systems>",
  "Avramenko Yevhenii <yevhenii.av@obox.systems>"
]
license = "MIT"

[dependencies]
tobj =  { workspace = true }
bytemuck = { workspace = true, features = [ "derive" ] } # Features already here
rand = { workspace = true } # Added features back (assuming default features are needed) # qqq : use workspace dependencies only
getrandom = { workspace = true, features = [ "js" ] } # Features already here # qqq : use workspace dependencies only

[dependencies.minwebgpu]
workspace = true
features = [ # Features already here
  'future',
  'file',
  'model_obj',
  'math'
]
