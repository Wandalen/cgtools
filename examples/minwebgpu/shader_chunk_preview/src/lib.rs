//! Library surface of the shader_chunk_preview example: the
//! native-testable [`shader_source`] assembler, exposed as a library so its
//! tests can live in `tests/`, per the all-tests-in-tests/ convention.

pub mod shader_source;
