//! Library surface of the sun-grid-lines WebGPU demo: the native-testable
//! [`scene`] configuration loader, exposed as a library so its tests can
//! live in `tests/`, per the all-tests-in-tests/ convention.

pub mod scene;
