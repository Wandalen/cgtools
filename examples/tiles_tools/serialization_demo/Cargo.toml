[package]
name = "serialization_demo"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
tiles_tools = { workspace = true, features = ["serialization"] }
serde_json = { workspace = true }
