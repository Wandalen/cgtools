[package]
name = "field_of_view_demo"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
tiles_tools = { workspace = true }

[package.metadata.action]
tags = ["runtime:native"]
