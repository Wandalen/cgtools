//! Library surface of the chunked sun-grid-lines demo: the manifest-driven
//! WGSL shader-chunk composer ( [`shader_chunks`] ), exposed as a library so
//! its tests can live in `tests/`, per the all-tests-in-tests/ convention.

pub mod shader_chunks;
