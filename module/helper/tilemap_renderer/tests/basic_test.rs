//! Basic integration tests for `tilemap_renderer` crate.

#![ allow( unused_imports ) ]
#![ allow( missing_docs ) ]
use tilemap_renderer as the_module;

/// Basic smoke test to verify the crate compiles and basic functionality works.
#[ test ]
fn test_crate_compiles()
{
  // Basic compilation test - will expand as we add functionality
}