[package]
name = "tilemap_renderer"
version = "0.2.0"
edition = "2024"
authors = ["cgtools contributors"]
license = "MIT"
description = "Agnostic 2D rendering engine with backend adapter support"
repository = "https://github.com/Wandalen/cgtools"

[lints]
workspace = true

[features]
default = []
adapter-svg = []
adapter-terminal = []
# Stubs for downstream crates — not yet implemented
adapter-webgl = []
adapter-wgpu = []
# CLI stub — not yet reimplemented
cli = []

[dependencies]
# Future backend dependencies (will be added when backends are implemented)
# minwebgl = { workspace = true, optional = true }
# wgpu = { workspace = true, optional = true }
# web-sys = { workspace = true, optional = true }
# bytemuck = { workspace = true, optional = true }

[dev-dependencies]
