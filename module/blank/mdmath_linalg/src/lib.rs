//! Linear algebra operations for multidimensional mathematics.
#![ doc( html_root_url = "https://docs.rs/mdmath_linalg/latest/mdmath_linalg/" ) ]
#![ cfg_attr( doc, doc = include_str!( concat!( env!( "CARGO_MANIFEST_DIR" ), "/", "readme.md" ) ) ) ]
#![ cfg_attr( not( doc ), doc = "Linear algebra operations for multidimensional mathematics" ) ]

use ::mod_interface::mod_interface;

mod private
{
  // use super::*;
}

crate::mod_interface!
{
}
