//! Frame orchestration: pass scheduling and render-target management (reserved).
#![ doc( html_root_url = "https://docs.rs/frame_graph/latest/frame_graph/" ) ]
#![ cfg_attr( doc, doc = include_str!( concat!( env!( "CARGO_MANIFEST_DIR" ), "/", "readme.md" ) ) ) ]
#![ cfg_attr( not( doc ), doc = "Frame orchestration: pass scheduling and render-target management (reserved)" ) ]

#[ cfg( feature = "enabled" ) ]
use ::mod_interface::mod_interface;

#[ cfg( feature = "enabled" ) ]
mod private
{
}

#[ cfg( feature = "enabled" ) ]
crate::mod_interface!
{
}
