//! Scene-as-script for the d3 rendering stack (reserved).
#![ doc( html_root_url = "https://docs.rs/d3_scene/latest/d3_scene/" ) ]
#![ cfg_attr( doc, doc = include_str!( concat!( env!( "CARGO_MANIFEST_DIR" ), "/", "readme.md" ) ) ) ]
#![ cfg_attr( not( doc ), doc = "Scene-as-script for the d3 rendering stack (reserved)" ) ]

#[ cfg( feature = "enabled" ) ]
use ::mod_interface::mod_interface;

#[ cfg( feature = "enabled" ) ]
mod private
{
}

#[ cfg( feature = "enabled" ) ]
crate::mod_interface!
{
}
