/// Internal namespace.
mod private
{
  // use crate::*;
}

crate::mod_interface!
{
  /// Vertex buffer layout
  layer vertex_buffer;
  /// Vertex Attribute layout
  layer vertex_attribute;
  /// Bind group layout
  layer bind_group;
  /// Pipeline layout
  layer pipeline;
}
