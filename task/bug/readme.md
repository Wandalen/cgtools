<!-- bug_system_metadata
type: integrated
version: 1.0
-->

# bug

Bug reports for the cgtools workspace. IDs share the tsk Unified ID namespace tracked in
`../readme.md` (`highest_id`).

## Responsibility Table

| File | Responsibility |
|------|-----------------|
| readme.md | Bug index and open bugs tracking |
| draft/ | Newly filed bugs, structurally incomplete |
| unverified/ | Structurally complete bugs awaiting the VERIFY gate |
| verifying/ | Bugs actively undergoing the VERIFY gate |
| verified/ | Bugs confirmed and claimable for fix work |
| executing/ | Bugs with an in-progress fix |
| executed/ | Bugs whose fix landed, awaiting acceptance review |
| accepting/ | Bugs under acceptance review |
| completed/ | Bugs whose fix is verified and closed |
| cancelled/ | Bugs closed as won't-fix or duplicate |
| mixed/ | Bugs with cross-boundary or entirely-foreign fix scope |
| orphan/ | Mixed bugs confirmed for full external relocation |

## Open Bugs

| ID | Title | Severity | Component | Filed | State |
|----|-------|----------|-----------|-------|-------|
| BUG-114 | [`diamond` example's uv `BufferDescriptor` uses `.stride( 3 )` on tightly-packed 2-component texture-coordinate data](./verified/114_diamond_uv_buffer_stride_mismatch.md) | High | examples/minwebgl/diamond | 2026-08-15 | 🎯 Verified |

## Closed Bugs

| ID | Title | Severity | Component | Filed | Closed | Root Cause | Round | Accepted By |
|----|-------|----------|-----------|-------|--------|------------|-------|-------------|
| BUG-007 | [csgrs's mandatory core2 dependency is permanently yanked](./completed/007_csgrs_core2_yanked_dependency.md) | Critical | workspace root Cargo.toml | 2026-08-08 | 2026-08-08 | core2 ^0.4 (csgrs's mandatory dep) is entirely yanked | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ |
| BUG-043 | [`Vector<E,4>::w()` returns the `z` component instead of the `w` component](./completed/043_vector_w_wrong_index.md) | Medium | module/math/ndarray_cg | 2026-08-09 | 2026-08-09 | `w()`'s body copy-pasted from `z()`; index never bumped from `2` to `3` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-046 | [`skeleton_tests.rs`'s shared setup helper fails to compile — missing `Node` import and nonexistent `gltf.scene` field](./completed/046_skeleton_test_compile_errors.md) | High | module/helper/renderer | 2026-08-09 | 2026-08-09 | Missing `Node` import + `gltf.scene` (singular) should be `gltf.scenes` (plural) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-050 | [`Tuple2IterMut`/`Tuple3IterMut`/`Tuple4IterMut`'s shared `index` cursor aliases `&mut` references under mixed-direction iteration](./completed/050_mdmath_core_itermut_shared_cursor_aliasing.md) | High | module/math/mdmath_core | 2026-08-10 | 2026-08-10 | `next()` and `next_back()` each map the shared `index` cursor to a tuple field independently, never cross-checked, so interleaving both on one iterator re-yields a field already handed out as a live `&mut` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-051 | [`BindGroupLayoutEntry`'s conversion to `web_sys` panics on `BindingType::Other`, its own documented default](./completed/051_bind_group_layout_entry_panics_on_documented_placeholder.md) | High | module/min/minwebgpu | 2026-08-10 | 2026-08-10 | Infallible `From` conversion panicked on `Other`, the type's own documented default/placeholder variant | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-052 | [`minwebgl::geometry::Positions::new` panics on an unsupported `natoms` instead of returning `WebglError`](./completed/052_geometry_natoms_unsupported_panic.md) | High | module/min/minwebgl | 2026-08-10 | 2026-08-10 | `match typ.natoms`'s `_` arm used `panic!` instead of returning through the function's own `Result< Self, WebglError >` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-053 | [An explicit `RUSTFLAGS` override silently disables `web_sys_unstable_apis`, flipping `get_image_data`/`MouseEvent` accessors between two incompatible web-sys signatures](./completed/053_web_sys_unstable_apis_rustflags_override.md) | High | module/min/minwebgl + module/helper/browser_input + 3 examples | 2026-08-10 | 2026-08-10 | An explicit `RUSTFLAGS` env var completely replaces (never merges with) `.cargo/config.toml`'s `[build] rustflags`, silently flipping `web_sys_unstable_apis` and the web-sys method signatures it gates | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-054 | [`[E]`'s `ArrayMut::vector_mut` casts via `as_ptr()` instead of `as_mut_ptr()`, producing a `&mut` reference with `SharedReadOnly` provenance](./completed/054_slice_vector_mut_shared_provenance_ub.md) | High | module/math/mdmath_core | 2026-08-10 | 2026-08-10 | Copy-pasted from the immutable `array_ref()` sibling without switching `as_ptr()` to `as_mut_ptr()`, leaving the returned `&mut` retagged `Unique` from a `SharedReadOnly`-tagged pointer | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-079 | [`getrandom` resolves to two incompatible major versions on `wasm32-unknown-unknown`, breaking every `--all-targets` build that pulls in `test_tools` or the `derive_tools`→`strum`→`phf` proc-macro chain](./completed/079_getrandom_wasm32_backend_version_split.md) | Medium | workspace root Cargo.lock + .cargo/config.toml | 2026-08-11 | 2026-08-11 | Cargo.lock holds getrandom 0.2/0.3/0.4 side by side; `.cargo/config.toml`'s `getrandom_backend="wasm_js"` cfg only reaches 0.3+, leaving the 0.2 generation (via `test_tools` → rand 0.8) backend-less on wasm32 — fixed with the renderer-established target-gated `features = ["js"]` dev-dependency shim in 5 crates | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-080 | [`mingl::geometry::BoundingBox`'s `#[non_exhaustive]` breaks 7 struct-literal construction sites across 2 crates, including the default `cargo check --workspace` build](./completed/080_bounding_box_non_exhaustive_breaks_workspace_check.md) | High | module/min/mingl + module/helper/primitive_generation + examples/minwebgl/text_rendering | 2026-08-11 | 2026-08-11 | `5f33be66` added `#[non_exhaustive]` to a 100%-pub-field struct without auditing downstream struct-literal constructors; 7 sites across 2 crates broke — fixed by switching all 7 to the already-established `BoundingBox::new` (text_rendering ×5 by the 058 sweep; ufo.rs ×2 landed via `96bb2aef`) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-091 | [`minwebgl::texture::d2`'s `get_image_data` call fails `clippy::cast_lossless` under `-D warnings`](./completed/091_minwebgl_get_image_data_cast_lossless_clippy.md) | Medium | module/min/minwebgl | 2026-08-11 | 2026-08-11 | `as f64` widening casts where `f64::from` exists, flagged by `cast_lossless`; reachable only under a `RUSTFLAGS` override (the branch is `cfg( not( web_sys_unstable_apis ) )`-gated post-BUG-053) — fixed with clippy's own `f64::from(...)` suggestion | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-101 | [`animation_surface_rendering`'s pinned `kurbo`/`peniko` versions no longer match the unpinned `interpoli` git dependency's current requirements, breaking `cargo check --workspace`](./completed/101_animation_surface_rendering_kurbo_peniko_version_skew.md) | High | examples/minwebgl/animation_surface_rendering | 2026-08-12 | 2026-08-13 | Local `kurbo`/`peniko` overrides actually already matched `interpoli`'s real requirement (double-version split traced to unrelated `primitive_generation`→`norad` font path); exact symptom no longer reproduced at the same unpinned `interpoli` commit — pinned `interpoli` to `rev = "04ae4a48..."` in root `Cargo.toml` to remove the non-determinism regardless | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-103 | [`sch help` succeeds silently and `sch compose help` misparses `help` as a chunk name — every help invocation is silent or wrong](./completed/103_sch_help_invocations_silent_or_misparsed.md) | Medium | module/shader/shader_chunks | 2026-08-13 | 2026-08-13 | `main.rs` never printed `result.outputs` (each routine printed its own success content), so every framework help path succeeded silently, and no mapping existed from `help`/`<command> help` spellings — a trailing `help` bound as a chunk argument. Fixed with central outputs printing + `cli_fmt`-rendered per-command help (unknown targets still fail loudly via `.{target}.help`) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-108 | [`sch`/`shader_chunks` panics on a closed stdout pipe — violating the crate's own "never a panic" contract](./completed/108_sch_stdout_broken_pipe_panic.md) | Medium | module/shader/shader_chunks | 2026-08-13 | 2026-08-13 | All stdout writes went through `println!`/`eprintln!`, which panic on any write error including `EPIPE` — a closed reader pipe (e.g. `sch list \| head -1`) triggered a raw panic instead of a quiet exit | 0 | user1@w002 (self) |
| BUG-109 | [mingl's document-relative asset URL resolution (`file::load`, `dom::image_element_create`, `dom::video_element_create`) joins against the origin root instead of the current page's own directory, breaking every example deployed under a subpath](./completed/109_file_load_resolves_relative_paths_against_origin_not_page_dir.md) | Critical | module/min/mingl (src/web/) + examples/minwebgl/{filter,mapgen_tiles_rendering} | 2026-08-13 | 2026-08-13 | `window.location().origin()` never includes a path component; relative asset references were joined against it alone instead of the current document's own directory — only correct for pages served at the domain root, false for this repo's per-example-subpath deployment. First fix covered only `file.rs`; an independent review reopened the bug when `dom.rs`'s textually-identical pattern (plus an unrelated inline duplicate in `filter` and a separate missing-`static/`-prefix bug in `mapgen_tiles_rendering`) was found still broken. Shared join logic consolidated into `web.rs`, all call sites fixed, all 7 affected examples live-reverified in a real browser | 1 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-110 | [`geometry_tests.rs`'s wasm32 suite is missing `wasm_bindgen_test_configure!( run_in_browser )`, so it silently ran under Node.js and failed with an unrelated-looking error](./completed/110_geometry_tests_missing_run_in_browser_config.md) | Medium | module/helper/renderer | 2026-08-13 | 2026-08-13 | Test file created (`9b71cf39`) without the `run_in_browser` configure! call that 3 sibling suites in the same directory already carry — the binary silently defaulted to Node.js execution, where `web_sys::window()` is always `None`, so `gl::canvas::make()` failed with `CanvasRetrievingError("Failed to get window")` before the test's own assertions ever ran | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/ (self) |
| BUG-115 | [`shader_chunks_query_core`'s documented `width::` truncation is silently overridden by `data_fmt`'s auto-wrap once a query's total row width exceeds 120 columns](./completed/115_query_markdown_width_truncation_overridden_by_auto_wrap.md) | Medium | module/shader/shader_chunks_query_core | 2026-08-15 | 2026-08-16 | `with_max_column_width` alone doesn't guarantee single-line truncation — `auto_wrap` (left at its default `true`) silently pre-empts it via `data_fmt`'s row-aggregate-width gate whenever the sum of capped column widths crosses the hardcoded 120-column terminal fallback; fixed by explicitly disabling `auto_wrap` in the `Markdown` arm | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-116 | [`shader_chunks_query_core`'s `format::table` (plain) output truncates at `width::`, contradicting `docs/cli/format/01_table_plain.md`'s documented wrap-onto-continuation-lines contract](./completed/116_query_table_plain_width_wraps_documentation_mismatch.md) | Medium | module/shader/shader_chunks_query_core | 2026-08-15 | 2026-08-16 | `should_auto_wrap` gates wrap-vs-truncate on a row's aggregate capped width vs. the 120-column fallback, not on any individual cell — a short `name` column can keep a row's total under that threshold even when `description` alone exceeds `width::`, so `truncate_cell` silently truncates instead of wrapping; fixed by manually pre-wrapping every `Table`-arm cell via `WrapFormatter`, bypassing the gate entirely | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-249 | [`shader_chunks_query`'s `docs/cli/param/21_width.md` still claimed `width::` truncates in `table` format, contradicting BUG-116's fix (`table` now wraps)](./completed/249_width_param_doc_still_claimed_table_truncates_after_bug116.md) | Low | module/shader/shader_chunks_query | 2026-08-15 | 2026-08-16 | `21_width.md` was never updated when BUG-116 split `table`'s and `markdown`'s `width::` contracts apart — a documentation-only staleness aftershock, not a code defect; fixed by rewriting Purpose/Example/Notes to state the format-specific contract; renumbered from 117 (bug/task ID collision with TASK-117, 2026-08-17) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-250 | [`Mat4::decompose()` divides by the scale reciprocal instead of multiplying, re-squaring scale into the extracted rotation matrix](./completed/250_decompose_scale_resquared_into_rotation_matrix.md) | High | module/math/ndarray_cg | 2026-08-15 | 2026-08-16 | `inv_scale` already holds the reciprocal (1/sx,1/sy,1/sz) — dividing by it instead of multiplying re-multiplied scale back into the extracted rotation matrix instead of removing it; fixed by changing `/ inv_scale` to `* inv_scale`; renumbered from 118 (bug/task ID collision with TASK-118, 2026-08-17) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-119 | [`Quat::from(Mat3)` writes the trace-derived `w` term into the `x` slot, cyclically shifting all four quaternion components](./completed/119_quat_from_mat3_cyclic_component_shift.md) | High | module/math/ndarray_cg | 2026-08-15 | 2026-08-16 | the final quaternion array was assembled in `[n0,n1,n2,n3]` order but `Quat` stores `[x,y,z,w]`, cyclically shifting all four components; fixed by reordering to `[n1,n2,n3,n0]`-based slots matching the storage convention | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-120 | [`Quat::from_axis_angle` uses the full angle instead of the half angle, producing a rotation twice the requested amount](./completed/120_from_axis_angle_missing_half_angle.md) | High | module/math/ndarray_cg + module/helper/renderer | 2026-08-15 | 2026-08-16 | `from_axis_angle` computed `sin_cos` on the full angle instead of the half angle, producing a rotation twice the requested amount; fixed by halving the angle before `sin_cos`, matching sibling constructors — renderer's own `scaler_tests.rs` only asserts the scale factor, not the affected rotation value, so it can't catch this regression class | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-121 | [`Mat * Vector` multiplication produces a wrong-length, silently-truncated result for any non-square matrix](./completed/121_mat_vec_mul_wrong_output_length_for_non_square.md) | Medium | module/math/ndarray_cg | 2026-08-15 | 2026-08-16 | `mat_vec_mul` reused a single `const ROWS` generic for both input length (matrix columns) and output length (matrix rows), which only coincidentally matched for the crate's exclusively-square-matrix callers; a non-square `Mat<M,N>` silently truncated the output to the input's length; fixed by splitting into independent `IN`/`OUT` generics and correcting both `Mul<Vector>` impls' `Output` type | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-122 | [`Tuple{2,3,4}Iter::next_back()` returns the wrong element under mixed `.next()`/`.next_back()` traversal](./completed/122_tuple_iter_next_back_shared_cursor_wrong_elements.md) | Medium | module/math/mdmath_core | 2026-08-15 | 2026-08-16 | `next()`/`next_back()` shared a single `index` cursor with direction-hardcoded match arms — mixing both on one iterator re-yielded/skipped elements; fixed with independent `front`/`back` cursors mirroring the already-fixed `*Mut` sibling (BUG-050) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-123 | [`[E]::vector_iter_mut` mutates past the logical vector length `N` on longer slices](./completed/123_slice_vector_iter_mut_missing_take_n_bound.md) | Medium | module/math/mdmath_core | 2026-08-15 | 2026-08-16 | `vector_iter_mut` omitted the `.take(N)` its `vector_iter` sibling applies, letting mutation reach elements beyond the logical vector length `N` on slices longer than `N` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-124 | [`vector::normalize(r, a)` writes `r`'s own values scaled by `a`'s magnitude, never reading `a`'s direction](./completed/124_vector_normalize_ignores_source_vector.md) | Medium | module/math/mdmath_core | 2026-08-15 | 2026-08-16 | `normalize`'s write loop dereferenced only `r`'s own elements, never constructing `a.vector_iter()` — correct only by coincidence when the caller pre-seeds `r = a` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-125 | [`CameraOrbitControls::update` treats seconds `delta_time` as milliseconds, scaling smoothed rotation down by 1000x](./completed/125_camera_orbit_update_delta_time_unit_mismatch.md) | Medium | module/min/mingl | 2026-08-15 | 2026-08-16 | Both internal formulas were written and doc-commented in terms of milliseconds ("every 10 milliseconds") but consumed the raw seconds-typed `delta_time` parameter with no conversion — fixed by converting to `delta_time_ms` once, up front | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-126 | [`CameraOrbitControls::zoom`'s zoom-out divisor can reach zero or go negative, corrupting or flipping the camera through its pivot](./completed/126_camera_orbit_zoom_unclamped_divisor_sign_flip.md) | Medium | module/min/mingl | 2026-08-15 | 2026-08-16 | The zoom-out branch's divisor `1.0 - delta_y.abs()` had no lower bound, unlike the zoom-in branch's safe-by-construction `1.0 + delta_y.abs()` — a single event with `\|delta_y\| >= zoom.speed` (reachable via real pinch/wheel input) drove it to zero or negative; fixed by flooring at `f32::EPSILON` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-127 | [`path_to_points` panics via `unreachable!()` on any closed path](./completed/127_path_to_points_panics_on_closed_path.md) | High | module/helper/primitive_generation | 2026-08-15 | 2026-08-16 | `path_to_points`'s flatten closure matched only `MoveTo`/`LineTo` and treated `ClosePath` as `unreachable!()`, but `kurbo::flatten` always re-emits `ClosePath` for any subpath that closes (the normal case for filled shapes) — reachable via the crate's own `lottie_surface_rendering` example; fixed by handling `ClosePath` as a no-op | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-128 | [`Glyph::from_glif` never recognizes any `.glif` point's declared type](./completed/128_glif_point_type_attribute_typo.md) | High | module/helper/primitive_generation | 2026-08-15 | 2026-08-16 | the point-attribute match looked for `b"typ"` instead of the real UFO/glif spec attribute `b"type"` — a one-letter typo that could never match, silently defaulting every point to `PointType::Move` and collapsing curves/lines into disconnected single-point contours; fixed by correcting the byte-string literal | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-129 | [text glyph layout over-advances by half a slot-width per glyph](./completed/129_text_layout_asymmetric_advance_step.md) | Medium | module/helper/primitive_generation | 2026-08-15 | 2026-08-16 | pass 2 of the two-pass centering layout advanced by each glyph's full slot-width in one step instead of a symmetric half-step-place-half-step mirroring pass 1, over-advancing and compounding the drift across the string; fixed identically in both `text_to_mesh` and `text_to_countour_mesh` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-130 | [pointer/button state tracking assumes one button per pointer at a time](./completed/130_pointer_button_state_ignores_pointer_identity.md) | Medium | module/helper/browser_input | 2026-08-15 | 2026-08-16 | `mouse_buttons`/`active_pointers` were updated as flat overwrite/unconditional-evict logic keyed only by button or pointer id, assuming exactly one button is ever held by one pointer at a time — broke under 2 simultaneous touches sharing `Main`, or one mouse holding 2 buttons; fixed with a per-pointer `held_buttons` bitmask both fields now derive from | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-131 | [`RectangularGrid<Parity, Flat>::center()` guards on the wrong axis, copy-pasted from the sibling `Pointy` impl](./completed/131_rectangular_grid_flat_center_wrong_axis.md) | Medium | module/helper/tiles_tools | 2026-08-16 | 2026-08-16 | `Flat`'s `center()` guard compared `.r` (the sibling `Pointy` impl's axis) instead of `.q`, even though its own candidate point already varied `q` — copy-paste left a stale axis reference; fixed by changing both guards to `.q` comparisons | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-132 | [`Animation::update` discards frame-timer overshoot beyond the first crossing](./completed/132_animation_update_discards_frame_timer_overshoot.md) | Medium | module/helper/tiles_tools | 2026-08-16 | 2026-08-16 | a single `if` consumed at most one `frame_duration` per call and unconditionally reset `frame_timer` to 0.0, discarding overshoot instead of carrying it forward under a large `dt`; fixed by converting to a `frame_duration > 0.0`-guarded `while` loop that subtracts per crossing | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-133 | [`turn_order_rebuild` desyncs `current_turn_index` from the acting entity](./completed/133_turn_order_rebuild_desyncs_current_turn_index.md) | Medium | module/helper/tiles_tools | 2026-08-16 | 2026-08-16 | the index was only numerically clamped against the rebuilt turn order's new length, never remapped to the entity it was tracking, so an add/remove mid-round could silently reassign whose turn it was; fixed by capturing the current entity's id before re-sort and remapping to its new position | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-134 | [`Quadtree::insert` lacks bounds validation, silently misfiling out-of-bounds entities](./completed/134_quadtree_insert_lacks_bounds_validation.md) | Medium | module/helper/tiles_tools | 2026-08-16 | 2026-08-16 | quadrant-routing comparisons are total over all of `i32` and never rejected a position outside the tree's declared bounds, so an out-of-bounds entity was filed into a leaf that couldn't be reached by `region_query`'s pruning; fixed by validating `bounds.contains_point` at the single entry point, returning `#[must_use] bool` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-135 | [`octant_shadows_cast` desyncs direction index from filtered neighbor position](./completed/135_octant_shadows_cast_index_desync.md) | Medium | module/helper/tiles_tools | 2026-08-16 | 2026-08-16 | `.filter().enumerate()` numbered only surviving (unvisited) neighbors, decoupling the loop index from each neighbor's true fixed slot in `pos.neighbors()` once any neighbor had already been visited; fixed by reordering to `.enumerate().filter()` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-136 | [`rectangle_query` filters by a circular full-diagonal radius, not a rectangle](./completed/136_rectangle_query_tests_circle_not_rectangle.md) | Medium | module/helper/tiles_tools | 2026-08-16 | 2026-08-16 | copy-pasted `circle_query`'s single-scalar distance-threshold shape, reinterpreting the radius as the rectangle's full diagonal — a circle roughly twice as generous as even the rectangle's own corner distance, not a fix; fixed with a genuine per-axis `\|dx\|<=w/2 && \|dy\|<=h/2` bounds check, narrowing to square coordinates | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-137 | [`EventResult::Unsubscribe` removes the listener but doesn't stop the event](./completed/137_unsubscribe_does_not_halt_event_propagation.md) | Medium | module/helper/tiles_tools | 2026-08-16 | 2026-08-16 | the `Unsubscribe` match arm queued the listener for removal but never `break`, so the current event kept propagating to lower-priority listeners, contradicting its own doc comment and the sibling `Consume` arm's behavior; fixed by adding `break;` to match `Consume` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-138 | [`Sequence::update` selects one player too far ahead via a `binary_search_by` off-by-one](./completed/138_sequence_update_binary_search_off_by_one.md) | High | module/helper/animation | 2026-08-16 | 2026-08-16 | the `binary_search_by`'s `Err( id )` arm used the insertion index directly instead of `id.saturating_sub( 1 )`, selecting the player one slot too far ahead whenever the target delay falls strictly between two players' delays | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-139 | [`Sequence::update`'s `Equal` arm feeds a reconstructed absolute elapsed into the additive `update()`](./completed/139_sequence_update_equal_arm_double_accumulates_elapsed.md) | High | module/helper/animation | 2026-08-16 | 2026-08-16 | the `Equal` match arm reconstructed an absolute elapsed value from the current player's state and fed it back into the additive `current.update()`, double-accumulating elapsed time instead of passing the raw per-frame `delta_time` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-140 | [`Tween::progress()` double-subtracts delay, undercounting progress on any delayed tween](./completed/140_tween_progress_double_subtracts_delay.md) | Medium | module/helper/animation | 2026-08-16 | 2026-08-16 | `progress()` subtracted `delay` from an `elapsed` that had already been delay-adjusted by `update()`, undercounting progress on any tween with nonzero delay and never reaching full completion | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-141 | [`CubicBezier::apply`'s Newton-Raphson `slope` is not `x_get`'s actual derivative](./completed/141_cubic_bezier_newton_raphson_wrong_derivative.md) | Medium | module/helper/animation | 2026-08-16 | 2026-08-16 | the Newton-Raphson `slope` expression omitted the product-rule cross term of `x_get`'s true derivative, so the solver converged to a systematically wrong `t` for any non-symmetric control-point pair | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-142 | [`Tween::with_duration(0.0)` reintroduces the exact division-by-zero `new` guards against](./completed/142_tween_with_duration_zero_reintroduces_nan.md) | High | module/helper/animation | 2026-08-16 | 2026-08-16 | `with_duration` assigned its argument directly, bypassing the `duration.max( 0.001 )` floor `new` applies, reintroducing the exact zero-duration division `new`'s guard exists to prevent | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-143 | [`[Tween<T>; N]::progress()` reconstructs elapsed from an arbitrary fixed element instead of the group's own pace-setter](./completed/143_tween_array_progress_uses_arbitrary_element.md) | Medium | module/helper/animation | 2026-08-16 | 2026-08-16 | array `progress()` read a fixed element's own elapsed/duration instead of selecting the group's actual pace-setter (max-elapsed) element, producing wrong progress the moment tweens in the array desync | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-144 | [`BehaviorContext::update()` discards `delta_time`, always resampling real wall-clock time instead](./completed/144_behavior_context_update_ignores_delta_time.md) | High | module/helper/behaviour_tree | 2026-08-16 | 2026-08-16 | `update()` ignored its own `delta_time` parameter and resampled `Instant::now()` instead, breaking any caller that doesn't tick in real wall-clock time (fixed timestep, fast-forward, deterministic tests) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-145 | [`ParallelNode::execute()` never resets children on termination, unlike every sibling composite](./completed/145_parallel_node_never_resets_abandoned_children.md) | Medium | module/helper/behaviour_tree | 2026-08-16 | 2026-08-16 | every termination branch omitted the `self.reset()` call every sibling composite node makes, leaving abandoned `Running` children stuck mid-execution instead of restarting cleanly on the node's next activation | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-146 | [`RepeatNode::times(child, 0)` still executes its child once](./completed/146_repeat_node_zero_count_executes_child_once.md) | Medium | module/helper/behaviour_tree | 2026-08-16 | 2026-08-16 | the max-repeats completion check ran after the child executed rather than before it, so a `times(child, 0)` node always executed its child once before the zero-count guard had a chance to short-circuit | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-147 | [`Sequencer::insert` doesn't revive a `Completed` Sequencer, stranding newly-added players](./completed/147_sequencer_insert_after_completion_stuck.md) | Medium | module/helper/animation | 2026-08-16 | 2026-08-16 | the revival guard only matched `state == Pending`, excluding `Completed`, so inserting a new player into an already-`Completed` sequencer left the whole sequencer permanently stuck instead of reviving to `Running` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-148 | [`Vec<E>::interpolate` silently truncates to the shorter length instead of panicking on mismatch](./completed/148_vec_interpolate_silent_truncation_on_length_mismatch.md) | Medium | module/helper/animation | 2026-08-16 | 2026-08-16 | the `Vec<E>` `Animatable::interpolate` impl zipped to the shorter of the two vectors' lengths instead of asserting equal lengths like its `CubicHermite` sibling, silently masking length-mismatch bugs instead of panicking loudly | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-149 | [`Squad::apply` blends tangents 1/3 toward the endpoints instead of slerping them directly, producing the wrong mid-curve quaternion](./completed/149_squad_apply_extraneous_tangent_blend.md) | Medium | module/helper/animation | 2026-08-16 | 2026-08-16 | `apply` blended the tangent-pair slerp 1/3 of the way toward the start/end endpoints before combining — an extraneous step absent from the reference SQUAD formula — producing the wrong mid-curve quaternion despite correct boundary behavior | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-150 | [`duplicate_color_interpolate_as_stop` panics via an unguarded `Vec::remove` when there are more color changes than recorded threads](./completed/150_duplicate_color_interpolate_as_stop_oob_thread_removal_panic.md) | High | module/helper/embroidery_tools | 2026-08-16 | 2026-08-16 | the merge guard compared `self.threads().get(thread_index)` against `self.threads().get(thread_index-1)` with no bounds check — when both indices are out-of-bounds both calls return `None`, and `None == None` is `true`, so the guard was satisfied and `self.threads.remove(thread_index)` ran out-of-bounds; fixed by adding a `thread_index < self.threads().len()` guard, mirroring the sibling `stop_interpolate_as_duplicate_color`'s existing bounds-check convention | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-151 | [`pec_table_process` silently drops every first-seen color instead of recording it, misaligning `emb.threads()` against the color table](./completed/151_pec_table_process_drops_first_seen_colors.md) | High | module/helper/embroidery_tools | 2026-08-16 | 2026-08-16 | the cache-miss branch (first sighting of a `color_index`) computed the thread and cached it in `thread_map` but never called `emb.thread_add`/`values.push` — only the cache-hit branch (a repeat sighting) did, so every color's first occurrence vanished; fixed by adding the same `thread_add`/`values.push` pair to the cache-miss branch, matching the sibling `pec_colors_process`'s unconditional per-byte push | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-152 | [`pec_header_write` unconditionally drops the caller's first added thread from the written color table](./completed/152_pec_header_write_drops_first_thread.md) | High | module/helper/embroidery_tools | 2026-08-16 | 2026-08-16 | `pec_header_write` sliced `emb.threads()[1..]` before building the color table, unconditionally excluding the caller's own first thread — confused with `pec_threads()[0]`'s unrelated "invalid value" sentinel status, a property of the fixed default-palette array, not of the caller's own thread list; fixed by passing `emb.threads()` through unsliced | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-153 | [`mesh_def_generate`'s `TriangleList` arm panics on an index buffer whose length isn't a multiple of 3](./completed/153_mesh_def_generate_triangle_list_index_overrun_panic.md) | High | module/helper/tilemap_renderer | 2026-08-16 | 2026-08-16 | `count` (an index-buffer length) was never rounded down to a multiple of 3 before chunking by `step_by(3)`, and the trailing partial chunk indexed the buffer directly (`v[i+j]`) instead of via bounds-checked `.get()` like the position lookups two lines below; fixed by changing the index-buffer access to `.get()` with the same `valid = false; break;` fallback | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-154 | [`point_set`/`color_set` doc comments claim a panic the implementation never produces](./completed/154_point_set_color_set_doc_panic_mismatch.md) | Low | module/helper/line_tools | 2026-08-16 | 2026-08-16 | doc comments for `point_set`/`color_set` were copy-pasted from `point_get`'s accurate "Will panic if index is out of range" wording, but both methods actually guard via `.get_mut(index)` inside `if let Some(..)`, silently no-oping instead; fixed by correcting the doc text to describe the actual (and, for `point_set`, already-tested) no-op contract — no implementation code changed | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ |
| BUG-155 | [`shader_chunks_render`'s "shared unknown chunk text" test asserts the wrong (unprefixed) string](./completed/155_render_cli_test_unknown_chunk_text_missing_prefix.md) | Low | module/shader/shader_chunks_render | 2026-08-16 | 2026-08-16 | the test's hardcoded expected string was missing the `shader_chunks ` prefix the real, shared `PreviewCliError::UnknownChunk` text carries -- the sibling `shader_chunks_preview` test with the identical name already asserted the correct (prefixed) text and passed; fixed the assertion plus 2 stale CLI doc examples showing the same unprefixed text, no source change (behavior was already correct) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-156 | [`ObjectLayer::z_in_object` is documented as the layer draw order but is never read by the compile pipeline](./completed/156_z_in_object_never_applied_to_layer_draw_order.md) | Medium | module/helper/tilemap_scene | 2026-08-16 | 2026-08-16 | all 5 per-bucket layer-gathering sites in `compile/frame.rs` iterated each object's layer stack in raw declaration order -- `z_in_object`, documented as the intended draw order, was read nowhere in the compile pipeline; fixed with a shared `layers_in_z_order` helper (stable sort by `z_in_object`) applied at all 5 sites | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-157 | [`Scene::tick_into`'s `OneShot` completion phase uses the raw, non-canonical edge hex instead of the render path's canonicalized one](./completed/157_edge_oneshot_completion_diverges_from_render_canonicalization.md) | Medium | module/helper/tilemap_scene | 2026-08-16 | 2026-08-16 | `tick_into` computed position-dependent animation phase from `Placement::Edge`'s raw, possibly non-canonical `hex` field, while the render path canonicalizes via `canonical_edge` first -- breaking `declared_phase_seconds`'s documented byte-for-byte agreement between event timing and rendered output; fixed by canonicalizing in `tick_into` too | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-158 | [The join/body tangent computation shared by 5 `d2` shaders produces `NaN` at a ~180-degree cusp](./completed/158_join_tangent_nan_at_180_degree_cusp.md) | High | module/helper/line_tools | 2026-08-16 | 2026-08-16 | the shared `normalize( normalize(pointC-pointB) + normalize(pointB-pointA) )` formula, copy-pasted across `join_miter.vert`/`join_bevel.vert`/`join_round.vert`/`body.vert`/`body_terminal.vert`, sums to the zero vector at a ~180-degree cusp and `normalize(vec2(0,0))` is `NaN` in GLSL; fixed with a squared-length guard falling back to the incoming segment's own unit direction, applied identically at all 5 sites | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-159 | [`drawbuffers` panics with a raw, undocumented index-out-of-bounds message instead of an attributable one](./completed/159_drawbuffers_raw_oob_index_panic.md) | Medium | module/min/minwebgl | 2026-08-16 | 2026-08-16 | the only guard present (`checked_add` against `u32::MAX` on the attachment id) never rejects an ordinary out-of-range index like 16 -- that only overflows near `u32::MAX - COLOR_ATTACHMENT0` -- so the array index itself was never validated; fixed by extracting `color_attachment_index_validate`, returning `Result<usize, WebglError>` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-160 | [`sprite_upload` hardcodes `texStorage3D`'s mip level count to 8, silently failing to allocate storage for sprites smaller than 128x128](./completed/160_sprite_upload_hardcoded_mip_levels.md) | High | module/min/minwebgl | 2026-08-16 | 2026-08-16 | `levels` was hardcoded to 8, valid only when `max(sprite_width,sprite_height) >= 128`; smaller sprites raise `INVALID_OPERATION` (never checked -- WebGL errors aren't surfaced as `Result::Err` by wasm-bindgen), silently allocating no storage; the one real caller's 128x128 sprites happened to sit exactly on the boundary where 8 is still valid, masking the bug; fixed by extracting `mip_levels_for_dimensions`, computed from the real dimensions | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-161 | [`sprite_upload`'s row/column computation panics via divide-by-zero when `sprites_in_row` is 0](./completed/161_sprite_upload_sprites_in_row_divide_by_zero.md) | Medium | module/min/minwebgl | 2026-08-16 | 2026-08-16 | the row/column computation divided and modulo'd by the caller-supplied `sprites_in_row` field with no zero-guard; `SpriteSheet` has no constructor and is deliberately kept exhaustive for external struct-literal construction, so a caller-computed `sprites_in_row: 0` panicked via integer division-by-zero instead of a clear message; fixed by extracting `sprite_position`, returning `Result<(u32,u32), WebglError>` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-162 | [`adapter_request`/`device_request` panic on ordinary, spec-defined outcomes instead of returning `Result`](./completed/162_adapter_device_request_panic_instead_of_result.md) | High | module/min/minwebgpu | 2026-08-16 | 2026-08-16 | `requestAdapter()` resolving to `null` ("no adapter") and `requestDevice()`'s promise being rejected were both blindly `.unwrap()`ed instead of routed through `Result`, violating this crate's own written invariant; fixed by converting both functions to `Result`-returning with 2 new `ContextError` variants, propagated via `?` at `setup()` and all 5 real call sites | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-163 | [`format_to_size` panics on 10 of `GpuVertexFormat`'s 41 spec-defined variants](./completed/163_format_to_size_unhandled_vertex_format_variants.md) | High | module/min/minwebgpu | 2026-08-16 | 2026-08-16 | the match covered only the multi-component (`x2`/`x3`/`x4`) formats, leaving all 9 single-component formats plus `Unorm8x4Bgra` falling through to an unconditional panic; fixed by completing the match with all 41 variants, keeping a documented `unreachable!()` wildcard (required by `#[wasm_bindgen]`'s hidden `#[non_exhaustive]` marking, confirmed via real `E0004` compiler feedback) since the call site is inside an infallible `From` impl | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-164 | [`adapter_request`/`preferred_format` panic at the FFI boundary when the browser has no WebGPU support at all](./completed/164_navigator_gpu_unchecked_ffi_panic.md) | High | module/min/minwebgpu | 2026-08-16 | 2026-08-16 | `navigator.gpu()` is a raw, unchecked property getter that returns JS `undefined` (typed as `Gpu` regardless) when the browser has no WebGPU support at all, causing a raw uncaught JS `TypeError` at the wasm-bindgen FFI boundary one call before BUG-162's own logic runs; discovered empirically via BUG-162's own regression test; fixed with a shared `gpu_or_unsupported` helper checking `JsValue::is_undefined`, used by both `adapter_request` and the newly-`Result`-returning `preferred_format` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/verified/ (self) |
| BUG-165 | [`surface_configure` panics on a transient zero-size resize input](./completed/165_surface_configure_zero_size_panic.md) | Medium | module/min/minwgpu | 2026-08-16 | 2026-08-16 | `size` was forwarded unconditionally to `wgpu::Surface::configure`, which panics on a zero-area configuration (a normal event, e.g. a minimized window) rather than erroring; proven reachable by the crate's own `flecs_bouncing_circles` example already needing a hand-written guard against it; fixed by extracting a unit-testable `validate_size` precondition check, converting `surface_configure` to `Result`-returning | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-166 | [`readback::rgba8` silently mislabels channel order for `Bgra8Unorm`/`Bgra8UnormSrgb` textures](./completed/166_readback_rgba8_bgra_channel_mislabeling.md) | High | module/min/minwgpu | 2026-08-16 | 2026-08-16 | the format-validity check was byte-size-only (`block_copy_size == Some(4)`), silently accepting `Bgra8*` formats -- explicitly listed as supported in the function's own doc -- without ever correcting their native BGRA byte order, returning silently red/blue-swapped "RGBA8" output; fixed by replacing the check with an explicit 4-format allowlist and adding a `bgra_to_rgba_swizzle` helper applied for the two BGRA formats | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-167 | [`DebugLog` trait methods report their own `file:line`, not the real caller's](./completed/167_debug_log_trait_reports_own_location.md) | Medium | module/helper/browser_log | 2026-08-16 | 2026-08-16 | all 5 `DebugLog` convenience methods called a `log!`-family macro directly inside their own trait default body -- `file!()`/`line!()` are lexical, always resolving to that fixed internal line, never the real external caller's, regardless of the pre-existing `#[inline]`; fixed by adding `#[track_caller]` to all 5 and rewriting `debug_log` to bypass the macros, manually constructing/dispatching a `log::Record` via `Location::caller()` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-168 | [`panic::Config { with_location: false, .. }` never suppresses the panic location](./completed/168_panic_config_with_location_false_ineffective.md) | Medium | module/helper/browser_log | 2026-08-16 | 2026-08-16 | `hook_impl` built its wasm32 message from `PanicHookInfo::to_string()` unconditionally, whose `Display` impl always embeds `"panicked at {file}:{line}:{col}:"` -- `with_location` only ever gated a second, redundant location block appended on top, never the first one; fixed by extracting a `panic_message` helper that bypasses `Display` and reads the payload directly when `with_location` is false | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-169 | [`mod private` missing `#[cfg(feature = "enabled")]` breaks `--no-default-features`](./completed/169_mod_private_missing_feature_gate_breaks_no_default_features.md) | Medium | module/helper/browser_log | 2026-08-16 | 2026-08-16 | `mod private` referenced `crate::log`/`crate::panic`, submodules only ever declared by the `mod_interface!` invocation's `layer log;`/`layer panic;` lines -- itself correctly `#[cfg(feature = "enabled")]`-gated, but `mod private` was left unconditional, so `--no-default-features` hit 4 `E0433` "cannot find `log`/`panic` in `crate`" errors; fixed by adding the identical gate to `mod private` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-170 | [`mod_interface` import/invocation ungated, breaks `--no-default-features`](./completed/170_ndarray_tools_mod_interface_ungated_breaks_no_default_features.md) | Medium | module/alias/ndarray_tools | 2026-08-16 | 2026-08-16 | `use ::mod_interface::mod_interface;` and the `crate::mod_interface! { reuse ::ndarray_cg; }` invocation were both fully unconditional despite `mod_interface`/`ndarray_cg` being optional dependencies gated behind `enabled` -- `--no-default-features` hit `E0432` "unresolved import `mod_interface`" on the very first line; same defect class as BUG-169, independent occurrence; fixed by gating both the import and the invocation | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-171 | [`Node` panics on `.inverse().unwrap()` for zero-scale (singular) transforms](./completed/171_node_zero_scale_singular_matrix_panics.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | `world_matrix_set`/`upload`/`local_bounding_box_hierarchical` all unwrapped `Option<Mat>::None` from `.inverse()`, panicking the entire per-frame render loop for any node whose accumulated scale has a zero on any axis (a common glTF flatten/hide pattern or scale-to-zero animation); fixed all 3 sites to fall back to identity | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-172 | [glTF `Direct`/`Spot` light direction derived from node translation instead of rotation](./completed/172_gltf_light_direction_from_position_not_rotation.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | `light_get`'s `Direct` arm only used the correct rotation-based direction formula when the node's translation magnitude was within 1cm of the origin, and `Spot` had no rotation-based fallback at all -- both derived `direction` from world position instead; fixed both arms to unconditionally compute direction from `node.rotation_get()`'s local -Z axis, per `KHR_lights_punctual` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-173 | [glTF skinning joints resolved by node name instead of index, silently dropping/colliding joints](./completed/173_gltf_skinning_joints_resolved_by_name_not_index.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | `skeleton_transforms_data_load` resolved joints via `joint.name()` against a name-keyed `FxHashMap`, silently dropping unnamed joint nodes and collapsing duplicate-named ones to whichever insertion happened last -- corrupting the positional `JOINTS_0`/`JOINTS_1` vertex-attribute binding; fixed to resolve via `joint.index()` against the flat, index-ordered node slice, matching every other node lookup in the same file | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-174 | [`Camera::new` performs no validation on `aspect_ratio`/`fov`/`near`/`far`](./completed/174_camera_new_no_guard_on_aspect_near_far.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | `Camera::new` fed `aspect_ratio`/`fov`/`near`/`far` straight into `perspective_rh_gl` with no validation -- `near==0` xor `far==0` produced an exactly-zero-determinant matrix that panicked several frames later via `.inverse().unwrap()` in `Renderer::skybox_draw`; every other degenerate case (zero/negative `aspect_ratio`, `fov<=0` or `>=PI`, `near>=far`) silently baked Inf/NaN into the projection matrix with no error signal at all; fixed by converting to `Result<Self, WebglError>` with domain guards on all 4 parameters, cascading to 17 downstream call sites workspace-wide | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-175 | [Spot-light shadow softness scaling is dead arithmetic](./completed/175_spot_light_shadow_softness_dead_arithmetic.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | `impl From<SpotLight> for Light`'s final `light_size` clamp used `.min( 0.01 )` where a lower-bound floor (`.max`) was intended -- the preceding cone-angle scaling term exceeds `0.01` for nearly every realistic `outer_cone_angle`, so `.min` always won and every spot light baked an identical, maximally-tight shadow penumbra regardless of cone angle; fixed by changing `.min( 0.01 )` to `.max( 0.01 )` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-176 | [`Device::texture_create` performs no validation on zero-sized dimensions](./completed/176_texture_create_zero_size_unguarded.md) | High | module/helper/gpu_hal | 2026-08-16 | 2026-08-16 | `desc.size` reached all 3 backend-specific texture-creation calls unvalidated -- native `wgpu::Device::create_texture` panics outright on a zero-component `Extent3d` (same defect class as BUG-165, different call path), WebGL's `tex_storage_2d` silently no-ops on `INVALID_VALUE` with the error never surfaced (same pattern as BUG-160), and WebGPU raises an uncaught validation error; reachable with no malformed caller input since a live canvas can transiently report zero width/height; fixed by adding a zero-component guard plus new `Error::InvalidInput` variant in the shared `gpu_hal` chokepoint, before the backend dispatch | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-177 | [`webgpu::Geometry::new` performs no vertex-count cross-validation](./completed/177_geometry_new_no_vertex_count_cross_validation.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | `vertex_count` was derived from `positions` alone and never cross-checked against `normals`/`uvs`/`colors`, despite the function's own doc comment stating all 4 arrays share "the same vertex count" -- a mismatched-length array silently built an undersized GPU buffer that a later draw call reads past the end of; fixed by adding a `positions.len() % 3` guard plus a length cross-check on the 3 other arrays, both returning `gpu_hal::Error::InvalidInput` (reusing the variant added in BUG-176) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-178 | [`color_grading.frag`'s white-balance `tint` shifts the wrong direction](./completed/178_color_grading_tint_inverted.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | `apply_white_balance` applied `tint`'s contribution with an opposing sign across the red/blue channels -- the same pattern correctly used for `temperature` (which is supposed to oppose), but magenta/green requires both channels to move *together*, so a positive ("magenta") `tint` value visibly shifted the image toward green and vice versa; fixed by matching `tint`'s sign on both channels while leaving `temperature`'s own correct opposing sign untouched | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-179 | [`WideOutlinePass::outline_thickness` never reaches the outline-decision shader](./completed/179_wide_outline_thickness_never_reaches_shader.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | `outline.frag` hardcoded `const float outlineThickness = 30.0;` instead of declaring it as a `uniform`, and `WideOutlineShader`'s location list never listed a matching entry -- so the pass's own `outline_thickness` field ( wired correctly into the JFA step's search radius ) never reached the final draw/no-draw distance comparison, which always used the fixed `30.0` regardless of what the caller configured; fixed by changing the GLSL declaration to a `uniform float` and adding the matching location lookup + per-frame upload in `outline_pass`, matching the existing `resolution` uniform's pattern | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-180 | [`WideOutlinePass`'s JFA step double-applies aspect-ratio correction, stretching outlines on non-square canvases](./completed/180_wide_outline_double_aspect_ratio_correction.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | `jfa_step_pass` scaled the JFA step's horizontal jump distance by `width / height` before upload, on top of `jfa_step.frag`'s own per-axis `/ resolution` conversion which already corrects for a non-square canvas -- double-applying the correction stretched the outline's search radius (and rendered shape) horizontally by a factor of the aspect ratio on any non-square canvas; fixed by removing the redundant Rust-side scaling so both `stepSize` components carry the same pixel distance, relying entirely on the shader's own per-axis division | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-181 | [`jfa_init.frag`'s silhouette detection fails for non-red objects](./completed/181_wide_outline_silhouette_fails_for_non_red_objects.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | the JFA silhouette check tested `objectColorTexture.r > 0.01`, assuming object pixels are red-dominant -- but `GBuffer::render` clears the OBJECT_COLOR attachment to `(-1,-1,-1,1)` and `gbuffer.frag` writes the caller's arbitrary `objectColor` uniform verbatim, so any object color with `r <= 0.01` (pure green/blue/cyan, or even black) was silently treated as background and received no outline at all; fixed by changing the comparison to `>= 0.0`, correctly discriminating any real non-negative color from the negative sentinel regardless of hue | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-182 | [`outline.frag`'s JFA seed-validity check tests the wrong sentinel value](./completed/182_outline_frag_seed_sentinel_wrong_comparison.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | the seed-validity check compared `seedCoord.x != 0.0 && seedCoord.y != 0.0` when the code's own comment names the actual sentinel as `-1.0` -- this both let the real `(-1,-1)` sentinel pass as "valid" (masked in practice by the resulting distance almost always exceeding `outlineThickness`) and incorrectly rejected any legitimately-found seed coordinate landing exactly on `0.0` on either axis; fixed by changing the comparison to `seedCoord.x >= 0.0 && seedCoord.y >= 0.0`, matching the sentinel the comment already named | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-189 | [`light_get` reads `KHR_lights_punctual` via the generic extensions catch-all, always returning `None`](./completed/189_light_get_reads_wrong_gltf_extension_accessor.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | the node-level light lookup manually re-parsed `gltf_node.extensions()`, the catch-all for extension data *unknown* to the crate -- but `KHR_lights_punctual` is a named, typed field this crate's `gltf-json` deserializes separately, so it never appeared there, making `light_get` return `None` unconditionally for every node; discovered while writing BUG-172's regression test; fixed to resolve via the crate's typed `gltf_node.light()?.index()` accessor, matching `light_list_get`'s existing correct pattern | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-193 | [`outline.frag` duplicates BUG-181's silhouette-detection threshold defect](./completed/193_outline_frag_object_present_duplicate_defect.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | `outline.frag` independently re-implements the same `objectColorTexture.r > 0.01` check BUG-181 fixed in `jfa_init.frag`, against the identical OBJECT_COLOR clear/write contract -- any object color with `r <= 0.01` fell through to the JFA distance path instead of drawing its own rendered color; discovered while diagnosing BUG-182 in the same file; fixed by changing the comparison to `>= 0.0`, reusing BUG-181's already-tested `object_is_present` model rather than duplicating it | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-183 | [`Blender::rotation_blend` NLERP sum has no hemisphere alignment](./completed/183_rotation_blend_nlerp_no_hemisphere_alignment.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | the accumulation loop summed each blended clip's rotation quaternion directly with no hemisphere check -- two clips whose current rotations land in opposite hemispheres (negative dot product) blended along the long rotational path instead of the short one; fixed by negating the incoming quaternion before accumulation whenever its dot product with the running sum is negative | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-194 | [`Quat::slerp` computes hemisphere correction but discards it](./completed/194_quat_slerp_discards_hemisphere_correction.md) | Medium | module/math/ndarray_cg | 2026-08-16 | 2026-08-16 | `slerp` correctly computed a hemisphere-corrected `q2` from a negative dot product, but the final interpolation expression referenced the original, uncorrected `other` parameter instead -- discovered while investigating BUG-183's sibling defect class; fixed by substituting `q2` into the interpolation expression | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-195 | [`blender_tests.rs`/`scaler_tests.rs` locally redefine wrong channel-name prefixes](./completed/195_blender_scaler_test_fixture_wrong_channel_prefix.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | both test files locally redefined `TRANSLATION_PREFIX`/`ROTATION_PREFIX`/`SCALE_PREFIX` with an underscore separator instead of importing the real, dot-separated production constants -- every `Sequencer::insert` key built from them silently missed `Blender`/`Scaler`'s own internal lookups, disabling all value-level test coverage in both files; discovered while writing BUG-183's regression test; fixed by importing the real constants from `base::` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-196 | [`rotation_blend` NLERP accumulator seeded from identity, not zero](./completed/196_rotation_blend_accumulator_seeded_from_identity.md) | Medium | module/helper/renderer | 2026-08-16 | 2026-08-16 | the accumulator started from `QuatF32::default()`, the IDENTITY quaternion, not the additive zero a weighted-sum-then-normalize accumulator needs -- silently mixing an unweighted "stay at identity" term into every blend, corrupting even a single fully-weighted clip; discovered while validating BUG-183's fix after BUG-195 unblocked the test; fixed by seeding the accumulator from the first entry itself | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-184 | [`Scaler::set` never applies translation/scale to grouped nodes](./completed/184_scaler_never_applies_translation_scale_to_grouped_nodes.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | `AnimatableComposition::set` for `Scaler` only ever called `scaled_rotation_apply` for grouped nodes -- `scaled_nodes`'s own documented `x` (translation) / `z` (scale) weight components were never wired to any apply function, so those channels stayed frozen at the node's default forever; real-caller GUI evidence confirmed the intended fix is weighted amplitude-scaling (mirroring rotation), not an unscaled passthrough; fixed by adding `scaled_translation_apply`/`scaled_scale_apply` and wiring both into `set()` | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-186 | [`scaled_rotation_apply` continuity rebase only ran when `scale < 1.0`](./completed/186_scaled_rotation_continuity_rebase_gated_on_scale_below_one.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | the per-segment continuity-rebase guard was `scale < 1.0 && i > 0`, an unrelated numeric clause narrowing when otherwise-unconditional bookkeeping ran -- any segment after the first scaled at `scale >= 1.0` (the GUI's own default and entire "amplify" range) kept a stale, un-rebased `start_value`, producing a visible discontinuity at every segment boundary; found while implementing BUG-184's sibling functions; fixed by changing the guard to unconditional `i > 0`, verified via a temporary direct-source-edit revert-and-rerun (test failed pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-187 | [Re-entering a graph node keeps stale elapsed time](./completed/187_reentering_graph_node_keeps_stale_elapsed_time.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | `AnimatableComposition::update`'s `is_transited` block synced a re-entered node's own persistent Sequencer via an additive `.update( time )` call with no preceding reset -- a node revisited after playing earlier still had its old elapsed time sitting there, so the sync added the transition's end time on top of that stale leftover instead of starting cleanly, compounding further with every additional re-entry; fixed by adding `next.animation.reset()` immediately before the sync, matching the codebase's own established reset-before-use idiom; verified via a temporary direct-source-edit removal-and-rerun (test failed pre-fix with an exact `got 6.5` match to the hand-derived value, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-185 | [`tweens[0].start_value` unconditionally clobbered by the last segment's raw end value](./completed/185_scaled_translation_first_segment_clobbered_by_last_segment_end_value.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | all 3 `scaled_*_apply` functions ended with an unconditional `tweens[0].start_value = tweens.last().unwrap().end_value` -- `tweens` is rebuilt fresh from the Sequencer every call and never persists, so this write was inert once `current` reached the last segment but actively corrupted the CURRENTLY SAMPLED tween's `start_value` with an unrelated last segment's raw, unscaled `end_value` whenever `current == 0`, the common case of a sequence's first segment still playing; found while implementing BUG-184's sibling functions' regression tests; fixed by deleting the line entirely, verified via a temporary direct-source-edit removal-and-rerun (test failed pre-fix with an exact `got (752.5, 750.0, 750.0)` match to the hand-derived clobbered value, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-198 | [Scaled channel elapsed time double-counted on local replay, playing at ~2x speed](./completed/198_scaled_tween_elapsed_doubled_on_local_replay.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | all 3 `scaled_*_apply` functions cloned tweens directly from the persistent, already-playing Sequencer (carrying forward real, non-zero `elapsed`), then drove the clones via a brand-new local `Sequence`'s absolute-time `.update( <channel>.time() )` with no reset in between -- `Sequence::new` resets only its own bookkeeping, never its players' state, so the already-non-zero elapsed and the freshly-applied absolute time compounded, roughly doubling effective elapsed and freezing playback at a segment's end pose once real elapsed reached only half its authored duration; discovered while writing BUG-185's regression test in the same functions (initially misfiled as BUG-197, an ID already informally allocated elsewhere -- renamed to BUG-198 after independent verification); fixed by adding a `tween.reset()` pass over every cloned tween immediately after cloning, before the rebase/scale loop, verified via a temporary direct-source-edit removal-and-rerun (test failed pre-fix with an exact `got (20.0, 0.0, 0.0)` match to the hand-derived doubled-elapsed value, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-188 | [A glTF animation channel with exactly one keyframe is silently dropped](./completed/188_single_keyframe_gltf_channel_silently_dropped.md) | High | module/helper/renderer | 2026-08-16 | 2026-08-16 | `quat_sequence`/`vec3_sequence`/`weights_sequence` each collect one `Tween` per keyframe and pass straight to `Sequence::new(tweens).ok()` -- `Sequence::new` correctly rejects fewer than 2 players (a structural requirement of the segment model), but a legitimately-authored single-keyframe channel ("hold this value for the whole clip") produces exactly 1 tween, and `.ok()` silently discards the resulting `Err` with no diagnostic, so the whole channel vanishes; fixed by duplicating the lone tween when `tweens.len() == 1`, satisfying the minimum while preserving the authored constant-value meaning; verified via a dispatched background agent's wasm32/browser test run (both directions) against a new hand-authored single-keyframe fixture | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-199 | [`Device::new_native` performs no validation on zero width/height](./completed/199_new_native_zero_size_unguarded.md) | High | module/helper/gpu_hal | 2026-08-16 | 2026-08-16 | `width`/`height` reached `wgpu::Device::create_texture`'s `Extent3d` unvalidated -- the same zero-size panic defect class already fixed once in this file for `texture_create` (BUG-176), missed here because `new_native` builds its own independent offscreen-surface texture through a separate construction site; reachable with entirely ordinary caller input (e.g. a resize event firing on a not-yet-laid-out or minimized window) since both real call sites already propagate `Result` with no guard of their own; fixed by adding a zero-component guard reusing BUG-176's `Error::InvalidInput` variant, before constructing the offscreen surface texture; verified via a temporary direct-source-edit revert-and-rerun (both new tests failed pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-200 | [`Queue::buffer_write`'s WebGL arm silently no-ops on oversized data](./completed/200_buffer_write_webgl_oversized_data_silent_noop.md) | High | module/helper/gpu_hal | 2026-08-16 | 2026-08-16 | `bufferSubData` (`buffer_sub_data_with_i32_and_u8_array`) returns `()` with no way to surface a GL error, and per the WebGL2 spec silently does nothing when `data` overflows the destination buffer's allocated size -- `buffer_write` had no length check and `BufferWebGl` didn't even track its own size, so it returned `Ok(())` unconditionally while the buffer kept stale/uninitialized contents; the same "WebGL error flag unchecked" pattern as BUG-160/BUG-176, and directly asymmetric with `texture_write`'s sibling call in the same file, which does correctly propagate its `Result`-returning binding; fixed by adding a `size` field to `BufferWebGl`, populating it at both construction sites, and guarding `buffer_write` with `Error::InvalidInput` (reusing BUG-176's variant) before the write; verified via a real Firefox/WebGL2 session (fail-then-pass, guard temporarily reverted then restored) against a new pixel-observable scenario added to the `triangle_browser` example, per this crate's own established browser-based (non-`cargo test`) verification convention | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-204 | [`Queue::texture_write`'s native arm performs no validation on undersized `data`](./completed/204_texture_write_native_undersized_data_unguarded_panic.md) | High | module/helper/gpu_hal | 2026-08-16 | 2026-08-16 | `wgpu::Queue::write_texture` is documented to fail when `data` is too short but returns `()`, not `Result`; with no custom `on_uncaptured_error` handler installed anywhere in this crate, the failure reaches wgpu-core's `default_error_handler`, which panics unconditionally -- the fourth occurrence of the unguarded-native-panic class already fixed at BUG-165/176/199, reachable through `tilemap_renderer`'s `assets_load` on any malformed/truncated bitmap asset despite that call chain already being fully `Result`-propagated; fixed by adding a required-byte-count guard (reusing BUG-176's `Error::InvalidInput` variant) to the native arm only -- the WebGPU/WebGL arms were investigated and found to already propagate their own errors correctly (WebGL's `tex_sub_image_2d` binding is `catch`-wrapped, unlike BUG-200's `bufferSubData`), so neither was touched; verified via a temporary direct-source-edit disable-and-rerun (test panicked pre-fix with wgpu's own predicted validation message, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-205 | [`value_chunk_harness_and_parameters`'s candidate selection prefers a chunk's own primitive over its dedicated `_preview` wrapper](./completed/205_value_chunk_prefers_primitive_over_dedicated_preview_wrapper.md) | Medium | module/shader/shader_chunks_preview_core | 2026-08-16 | 2026-08-16 | a `//@ param:` declaration is scoped to an argument name, not to one specific export -- when a primitive and its dedicated `NAME_preview` wrapper share a trailing argument name, both become viable, and the tie-break only ever checked "named like the chunk itself", which the primitive always satisfies by construction and the wrapper structurally never can; confirmed affecting `domain_warp` (visibly -- wrong write mode rendered) plus `d2_sdf_circle`/`d2_sdf_ring` (latently -- byte-identical output either way, since their wrappers are trivial passthroughs); discovered via a regression test already written by task #122's review agent; fixed by adding a `"{name}_preview"`-named preference tier before the existing exact-name tie-break; also corrected the crate's algorithm doc, independently stale since before the trailing-argument (`own_params`) feature | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-207 | [`Queue::buffer_write`'s native arm performs no validation on misaligned or oversized `data`](./completed/207_buffer_write_native_unvalidated_alignment_and_overrun.md) | High | module/helper/gpu_hal | 2026-08-16 | 2026-08-17 | `wgpu::Queue::write_buffer` is documented to require in-bounds, `COPY_BUFFER_ALIGNMENT`-aligned writes but returns `()`, not `Result` -- with no custom `on_uncaptured_error` handler installed, a violation reaches wgpu-core's `default_error_handler`, which panics unconditionally; fifth occurrence of the BUG-165/176/199/204 unguarded-native-panic class in this file; fixed with a new `native_buffer_write_len_validate` helper returning `Error::InvalidInput` before the write | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-208 | [`RenderPass::vertex_buffer_set`/`index_buffer_set`'s native arms panic on a zero-size buffer](./completed/208_vertex_index_buffer_set_native_zero_size_panic.md) | High | module/helper/gpu_hal | 2026-08-16 | 2026-08-17 | `wgpu::RenderPass::set_vertex_buffer`/`set_index_buffer` slice the bound buffer via `BufferSlice::size_expect_nonzero()`, documented to panic when the buffer's own allocated size is 0 -- reachable via an all-empty geometry with no `vertex_count>0` guard upstream; fixed by skipping the native bind call when `raw.size() == 0`, mirroring the WebGL arm's own existing no-op-when-empty convention in the same file | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-209 | [`cmd_mesh`/`cmd_sprite` silently drop draws referencing a never-loaded asset instead of erroring (SVG + WebGL)](./completed/209_svg_webgl_missing_asset_silently_dropped_instead_of_error.md) | Medium | module/helper/tilemap_renderer | 2026-08-16 | 2026-08-17 | SVG never tracked loaded-vs-unloaded asset state at draw time (fixed via new `geometries_known`/`sprite_defs` membership checks); WebGL already had queryable resource maps but never routed a lookup miss to `Err` (fixed by converting the silent `if let Some(..) = .. { .. }` fallthrough to an explicit `Err(RenderError::MissingAsset(..))`), matching the already-correct `native.rs`/`webgpu.rs` sibling contract; also surfaced and fixed 4 pre-existing tests whose malformed 4-byte bitmap fixtures were silently masking this exact bug | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-210 | [`bitmap_texture_upload` never sets `UNPACK_FLIP_Y_WEBGL`, so `ImageSource::Bitmap` sprites render upside-down](./completed/210_webgl_bitmap_upload_y_flip_asymmetry.md) | Medium | module/helper/tilemap_renderer | 2026-08-16 | 2026-08-17 | the async `ImageSource::Path` upload path sets `UNPACK_FLIP_Y_WEBGL=1` (via `minwebgl::texture::d2::upload`) but the sync `ImageSource::Bitmap` path (`bitmap_texture_upload`) never did, while the sprite shaders' UV formula assumes the flipped convention unconditionally; fixed by adding the matching `pixel_storei` set/restore, mirroring the function's own existing `UNPACK_ALIGNMENT` restore pattern; no live-`WebGl2RenderingContext` test exists in this crate, verified by direct source inspection only | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-211 | [SVG's `cmd_set_sprite_instance`/`cmd_set_mesh_instance` silently no-op on an out-of-bounds instance index instead of erroring](./completed/211_svg_set_instance_out_of_bounds_index_silent_noop.md) | Medium | module/helper/tilemap_renderer | 2026-08-16 | 2026-08-17 | a chained `if let ... && ...` conflated "no batch bound"/"wrong batch variant" (legitimate no-ops) with "index out of bounds" (a genuine caller bug) into one silent `Ok(())` fallthrough; fixed by splitting into explicit no-op guards followed by an explicit out-of-bounds `Err(RenderError::BackendError(..))` check, matching the already-correct `webgl.rs` sibling shape verbatim | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-212 | [`held_buttons` grows without bound, bypassing the cap `active_pointers` already enforces](./completed/212_held_buttons_unbounded_growth_bypasses_active_pointers_cap.md) | Medium | module/helper/browser_input | 2026-08-17 | 2026-08-17 | `held_buttons`' `Press`-arm insertion had no admission cap, unlike `active_pointers`' identical-purpose insertion in the same match arm -- a flood of distinct pointer ids could grow it unboundedly and leave `mouse_buttons` stuck `true` for ids `active_pointers` never admitted; fixed by gating `held_buttons` insertion behind the identical `already_tracked \|\| len() < MAX_ACTIVE_POINTERS` check `active_pointers` already used | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-213 | [`MouseButton::Unknown`/`KeyboardKey::Unidentified` fallback aliasing causes premature release when two distinct unmapped inputs are held at once](./completed/213_unknown_button_and_unidentified_key_aliasing_causes_premature_release.md) | Medium | module/helper/browser_input | 2026-08-17 | 2026-08-17 | both fallback variants collapse many distinct real inputs onto one flat hold-bit, so releasing one aliased input falsely cleared another still-held one; fixed with per-pointer/global hold-COUNTs for each variant, plus an `event.repeat()` filter on the keyboard DOM listener as a prerequisite (OS auto-repeat would otherwise desync a naive counter) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-214 | [No `blur`/`visibilitychange` handling leaves held keys/buttons stuck when the page loses focus](./completed/214_missing_focus_loss_handling_leaves_input_stuck_held.md) | Medium | module/helper/browser_input | 2026-08-17 | 2026-08-17 | `Input::new` registered no listener for any focus-loss signal, so a key/button held when the OS moved focus elsewhere never received its matching release and stayed stuck held; fixed with a new `EventType::FocusLost` variant queued by new `blur`(window)/`visibilitychange`(document) listeners, resetting all currently-held state while leaving last-known-value fields (`pointer_position`, `scroll`) untouched | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-215 | [`Glyph::from_glif`'s point type accumulator leaks across points within a contour](./completed/215_glif_point_type_leaks_across_points_within_a_contour.md) | Medium | module/helper/primitive_generation | 2026-08-17 | 2026-08-17 | `typ` was declared once per *contour* instead of per *point*, so a point omitting its `type` attribute (the spec-correct way to write an off-curve control point) silently inherited the previous point's type instead of defaulting to `OffCurve`; fixed by moving the declaration inside the point-parsing loop, defaulting fresh per point per `norad`'s own reference parser | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-216 | [`Font`'s bounding-box union uses lexicographic `Vector` ordering instead of component-wise min/max](./completed/216_font_bounding_box_union_uses_lexicographic_vector_ordering.md) | Medium | module/helper/primitive_generation | 2026-08-17 | 2026-08-17 | the union loops in `Font::from_glyphs`/`Font::new` used `Vector`'s `<`/`>` operators, which delegate to lexicographic (x-component-first) array comparison, instead of its component-wise `.min()`/`.max()` methods -- a glyph with a dominant x-extent could win the comparison wholesale and discard another glyph's true y-extent; fixed at both call sites plus a new `Font::max_size()` accessor to make the union testable | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-217 | [`primitives_data_to_gltf` never generates or uploads a normal attribute, causing NaN lighting](./completed/217_primitives_data_to_gltf_never_uploads_a_normal_attribute.md) | Medium | module/helper/primitive_generation | 2026-08-17 | 2026-08-17 | `AttributesData` had no `normals` field and `primitives_data_to_gltf` never bound a normal attribute, yet `PbrMaterial`'s vertex shader unconditionally `normalize()`s a required `layout(location=1) in vec3 normal` -- an unbound attribute reads WebGL's default `(0,0,0)`, and `normalize` of that is NaN; fixed by adding `AttributesData::normals` and populating it in all 3 geometry generators, each normal derived from that function's own actual triangle winding; also fixed 1 downstream example crate (`animation_surface_rendering`) whose direct `AttributesData` construction would otherwise no longer compile | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-227 | [`framebuffer_create`'s renderbuffer creation panics instead of returning `None` like its siblings](./completed/227_canvas_renderer_framebuffer_create_renderbuffer_unwrap_panic.md) | Medium | module/helper/canvas_renderer | 2026-08-17 | 2026-08-17 | `framebuffer_create`'s doc comment promises `None` on failure, and `create_texture()?`/`create_framebuffer()?` honor it, but `create_renderbuffer().unwrap()` panics on the identical WebGL context-loss failure class instead of propagating `None` like its siblings; fixed by changing `.unwrap()` to `?`; no live-`WebGl2RenderingContext` test exists in this crate, verified by direct source inspection only, matching BUG-210's established precedent | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-228 | [`ParallelNode` re-invokes already-succeeded children every tick, preventing convergence and colliding with sibling internal state](./completed/228_parallel_node_repolls_already_succeeded_children.md) | High | module/helper/behaviour_tree | 2026-08-17 | 2026-08-17 | `ParallelNode::execute` unconditionally re-invoked every child every tick with no memory of an earlier tick's `Success`, unlike `SequenceNode`/`SelectorNode`'s cursor-based skip -- a `WaitAction` child got silently restarted after succeeding, and a `CooldownNode` child re-polled after success returned `Failure` from being back inside its own cooldown window, wrongly short-circuiting the whole composite; fixed by adding a per-child `succeeded : Vec<bool>` flag that skips already-terminal children, verified via a temporary direct-source-edit revert-and-rerun (`left: Failure, right: Success` pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-229 | [`DebugLog`'s trait-default methods tag every `Record` with their own module path, not the real caller's, silently defeating `Config::target_filter`](./completed/229_debug_log_module_path_lexical_resolution.md) | Medium | module/helper/browser_log | 2026-08-17 | 2026-08-17 | `module_path!()`, called inside `DebugLog`'s trait-default method body, is lexical like `file!()`/`line!()` but has no `#[track_caller]`-equivalent, so every emitted `Record`'s `target`/`module_path` always resolved to `browser_log`'s own internal module regardless of caller -- silently defeating `Config::target_filter` (the crate's own documented primary use case) for every consumer who sets one; BUG-167's own fix comment explicitly noted this gap was left unaddressed; fixed by adding an explicit `target : &str` parameter callers fill with their own `module_path!()`, mirroring `log`'s own `target:` escape hatch for this identical limitation; also fixed 2 downstream example-crate call sites the initial scout's `browser_log::`-prefix grep missed (blanket-impl trait methods carry no such prefix at the call site), verified via a temporary direct-source-edit revert-and-rerun (`left: "browser_log::log::debug_log::private", right: "debug_log_test"` pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-230 | [`with_repeat`'s script-facing `i64`-to-`i32` cast silently wraps, and one wraparound value lands exactly on the infinite-repeat sentinel](./completed/230_scene_script_with_repeat_unguarded_i64_to_i32_cast.md) | High | module/helper/scene_script | 2026-08-17 | 2026-08-17 | all 8 `with_repeat` Rhai registrations in `tween_binding.rs` cast their script-supplied `i64` straight to `i32` via unchecked `as`, and `4294967295i64 as i32 == -1` -- exactly `Tween`'s documented infinite-repeat sentinel -- so a script author intending a large but finite repeat count could silently get an infinite tween instead, with no error; fixed by adding a shared `repeat_count_from_i64` helper (`i32::try_from`, script-catchable error on overflow) mirroring the file's own existing `easing_from_name` error convention, applied to all 8 registrations; no Rust-level API break (`animation::Tween::with_repeat`'s own signature untouched), verified via a temporary direct-source-edit revert-and-rerun (`Ok` value returned where an error was expected, pre-fix; passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-231 | [`Sequencer::remove()` never updates `self.state`, stranding an emptied Sequencer permanently at `Running`](./completed/231_sequencer_remove_last_player_leaves_state_stuck_running.md) | Medium | module/helper/animation | 2026-08-17 | 2026-08-17 | `remove()` only ever mutated `self.players`, never `self.state`, and `update()`'s completion check deliberately requires a non-empty player set before transitioning to `Completed` -- so removing the last player while `Running` left `state` permanently stuck at `Running`, with `update()` still accumulating `time` on every call; fixed by transitioning `Running` to `Pending` when a removal leaves the player set empty, mirroring `reset()`'s existing empty-means-`Pending` convention and BUG-147's own asymmetry (only auto-reached `Running` is superseded, never a caller-requested `Paused`), verified via a temporary direct-source-edit revert-and-rerun (`left: Running, right: Pending` pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-232 | [`Tween`'s finite `repeat_count` overshoots when a single large `delta_time` crosses more repeat boundaries than remain in the budget](./completed/232_tween_finite_repeat_overshoot_on_large_delta.md) | High | module/helper/animation | 2026-08-17 | 2026-08-17 | `repeat_handle`'s finite-repeat branch added a call's entire `elapsed_repeats` to `current_repeat` unconditionally with no check against `repeat_count`, so a single oversized `delta_time` (frame stall, backgrounded tab, fast-forward) could carry `current_repeat` past `repeat_count` while `state` incorrectly stayed `Running`; fixed by clamping the crossings against the remaining budget (`repeat_count - current_repeat`) and completing immediately (capped `current_repeat`, `elapsed = duration`) when a batch would exceed it, mirroring the existing "no repeats left" convention; an initial mis-hypothesis about the ordinary single-crossing timing was falsified by re-reading the existing `test_tween_finite_repeat`'s documented intent before filing; verified via a temporary direct-source-edit revert-and-rerun (assertion failure pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-233 | [`Step::new(0.0)`'s divide-by-zero silently produces `NaN` instead of erroring](./completed/233_step_easing_zero_steps_produces_nan.md) | Medium | module/helper/animation | 2026-08-17 | 2026-08-17 | `Step::new` stored its `steps` argument with no floor, and `apply`'s `( time * self.steps ).ceil() / self.steps` divides by it directly -- `f64` division never panics, so `steps == 0.0` silently produced `NaN` instead of erroring; fixed by flooring with `steps.max( 0.001 )` in `Step::new`, mirroring `Tween::new`'s already-established `duration.max( 0.001 )` guard against the identical division-by-zero shape (same precedent as BUG-142), verified via a temporary direct-source-edit revert-and-rerun (NaN produced pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-234 | [`pes_addendum_write` panics with "capacity overflow" for designs with more than 128 threads](./completed/234_pes_addendum_write_capacity_overflow_panic_over_128_threads.md) | High | module/helper/embroidery_tools | 2026-08-17 | 2026-08-17 | PES v6's addendum color-index field is a fixed 128-byte slot, but the only existing guard on its input length (`pec_header_write`'s own "too many color changes" check) allows up to 255 -- for `count` in `129..=255`, `128_usize.wrapping_sub( count )` silently underflowed to a value near `usize::MAX`, which as a `Vec<u8>` allocation size exceeds `isize::MAX` and panics with "capacity overflow" instead of returning a catchable error; fixed by adding an explicit `count > 128` guard returning `EmbroideryError::CompatibilityError`, mirroring `pec_header_write`'s own precedent, and replacing the `wrapping_sub` with a now-safe plain subtraction, verified via a temporary direct-source-edit revert-and-rerun (exact "capacity overflow" panic reproduced pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-235 | [`as_segment_blocks` panics with `unwrap()` on `None` for designs with zero threads](./completed/235_as_segment_blocks_empty_chart_unwrap_panic_zero_threads.md) | Medium | module/helper/embroidery_tools | 2026-08-17 | 2026-08-17 | `as_segment_blocks` builds `chart` directly from `emb.threads()`, then calls `nearest_color_find( ..., &chart ).unwrap()` at two call sites -- `nearest_color_find` is documented to return `None` for an empty/all-`None` palette, and a design with zero threads (no `thread_add` calls and no `Stitch`/`SewTo`/`NeedleAt` instruction for `color_count_fix` to backfill one) reaches this call with an empty `chart`, turning the documented `None` into an unconditional panic; fixed by changing both `.unwrap()` sites to `.unwrap_or( 0 )`, safe because this data feeds PES v6's CEmbOne/CSewSeg block which this codebase's own `pes::read` never reads back, mirroring `thread_or_filler_get`'s existing "substitute something reasonable instead of erroring" convention, verified via a temporary direct-source-edit revert-and-rerun (exact `unwrap()`-on-`None` panic reproduced pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-236 | [`Cap::Round( 0 ).geometry()` divides by zero, producing a `NaN` cap vertex](./completed/236_round_cap_geometry_zero_segments_produces_nan.md) | Medium | module/helper/line_tools | 2026-08-17 | 2026-08-17 | `round_cap_geometry`'s vertex loop computes `i as f32 / segments as f32` inside an inclusive `0..=segments` range that still runs once at `segments == 0`, and `f32` division never panics on a zero divisor -- silently pushing a `NaN` vertex into the returned cap mesh instead of erroring; `Cap::Round( usize )` is a bare public tuple variant with no validation anywhere in the call chain; fixed by flooring `segments.max( 1 )` at entry, mirroring BUG-233's `.max( .. )` convention, verified via a temporary direct-source-edit revert-and-rerun (exact `[0.0, 0.0, NaN, NaN]` reproduced pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-237 | [`circle_geometry( 0 )` divides by zero, producing a `NaN` vertex](./completed/237_circle_geometry_zero_segments_produces_nan.md) | Low | module/helper/line_tools | 2026-08-17 | 2026-08-17 | Same defect shape as BUG-236, found in the same scouting pass in a different function: `circle_geometry`'s inclusive `0..=segments` loop divides by `segments` with no floor, silently producing a `NaN` vertex at `segments == 0`; zero in-tree callers today (confirmed via workspace-wide grep) but a live public-API defect, natively testable with no GL context -- fixed by flooring `segments.max( 1 )` at entry, mirroring BUG-236/BUG-233, verified via a temporary direct-source-edit revert-and-rerun (exact `[[NaN, NaN]]` reproduced pre-fix, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-238 | [`Line::point_add_back`/`point_add_front` compare a squared distance against an unsquared `EPSILON`, silently dropping distinct points ~2900x closer than intended](./completed/238_line_point_add_squared_epsilon_mismatch_drops_points.md) | Medium | module/helper/line_tools | 2026-08-17 | 2026-08-17 | `impl_basic_line!`'s `point_add_back`/`point_add_front` gate their near-duplicate check on `( last - point ).mag2() <= $primitive_type::EPSILON` -- `mag2()` is a *squared* distance, so the actual dedup radius silently became `sqrt( f32::EPSILON )` (~3.45e-4) instead of the evidently-intended `f32::EPSILON` (~1.19e-7), ~2900x too permissive; existing tests only ever exercised exact-duplicate points (distance 0.0), which dedups correctly under both the buggy and fixed comparison, so the defect went unexercised; fixed by squaring the threshold (`EPSILON * EPSILON`) at both call sites, preserving the `mag2()` sqrt-avoidance optimization, verified via a temporary direct-source-edit revert-and-rerun (`left: 2, right: 1` -- the second point silently dropped -- reproduced pre-fix on both call sites, passed post-fix) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-239 | [`Transform::to_mat3()`'s Y-skew (`skew[1]`) term has an inverted sign, diverging from the SVG backend's own skew convention](./completed/239_transform_to_mat3_skew_y_sign_inverted.md) | Low | module/helper/tilemap_renderer | 2026-08-17 | 2026-08-17 | `to_mat3()`'s x-basis column (`m00`, `m10`) computed the `sky` (`skew[1].tan()`, "skewY") term with the opposite sign from the real SVG `skewY(a)` matrix (`x'=x, y'=y+x*tan(a)`) that `Transform::skew`'s own doc comment and `svg.rs`'s `transform_to_svg_local` (passes `skew[1]` straight into a real SVG `skewY()` op, unnegated) both define it against; `skew[0]` ("skewX") had no such error; no test ever set `skew` non-default and an exhaustive workspace grep found zero real callers do either, so the path was unexercised; confirmed via isolated single-axis numeric evaluation (rotation=0, scale=1, one skew field nonzero at a time) after two earlier full hand-algebra derivations produced self-contradictory results; fixed by flipping the `sky` sign in `m00`/`m10`, verified via 2 new regression tests plus all 5 pre-existing `to_mat3_*` tests passing unchanged (150/150 total, clippy clean) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-240 | [`NativeBackend`'s sprite quad ignores `region` pixel size and anchors at the sprite's center instead of a corner, diverging from WebGL/WebGPU/SVG](./completed/240_native_backend_sprite_quad_unscaled_by_region.md) | Low | module/helper/tilemap_renderer | 2026-08-17 | 2026-08-17 | `quad_vertices` fed its raw `[-0.5,0.5]` local quad corners into the transform's linear part directly, never scaling by `region`'s pixel width/height and never shifting off center -- `webgl.rs`'s `sprite.vert`, `webgpu.rs`'s WGSL `vs_main`, and `svg.rs`'s `sprites_load` all instead scale the local quad by `region` before the transform and anchor `Transform::position` at a corner, not the center; confirmed via 3 mutually-consistent cross-backend shader/SVG reads plus direct matrix-substitution proof for the anchor claim; zero real (non-test) `NativeBackend` callers exist today (confirmed via exhaustive grep) but the defect covers 100% of this backend's own sprite draws; fixed by reusing `quad_vertices`'s own pre-existing `fx`/`fy` (already computed for UV) to scale `world_x`/`world_y` by `region[2]`/`region[3]`, verified via 1 new regression test plus the pre-existing pixel test's fixture corrected to the new semantics, all passing on real GPU pixel readback (151/151 total, clippy clean) | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-241 | [`renderer`'s crate-root `mod_interface!` and unconditionally-included `webgl` layer require the `enabled` feature's dependencies even when disabled, breaking the documented no-browser `native`-only build](./completed/241_renderer_native_feature_missing_enabled_dependency.md) | High | module/helper/renderer | 2026-08-17 | 2026-08-17 | `mod_interface::mod_interface!` used unconditionally at the crate root (`lib.rs`) required `dep:mod_interface`, gated behind the `enabled` feature; compounded by `layer webgl;`'s `#[cfg(feature="webgl")]` being commented out, making the entire `enabled`-only WebGL stack unconditional too -- `--no-default-features --features native` (the crate's own documented "terminal pixel tests, no browser" configuration) failed with `E0433: cannot find mod_interface in the crate root`; fixed by making `mod_interface` a required (non-optional) dependency, restoring the `webgl` cfg gate, and wiring `webgl = ["enabled"]` + `default = ["enabled","webgl"]` so the pre-fix default build's behavior is preserved; verified via 4 feature-combination `cargo check` invocations (native-only, default, webgl-only, all-features), all exit 0 post-fix | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-242 | [`Blender::is_completed()` never implements its own "all animations completed" contract -- ties always report incomplete, non-ties check only one animation](./completed/242_blender_is_completed_wrong_aggregation_logic.md) | Medium | module/helper/renderer | 2026-08-17 | 2026-08-17 | Sorted animations by raw `.time()` and, when the top two were tied within an EPSILON (including when both were genuinely `Completed`), unconditionally returned `false`; when not tied, checked only the single animation with the largest `.time()` (meaningless across differing durations) and ignored the rest, producing false positives; masked by `Blender::update()`'s own auto-reset-on-completion erasing genuine `Completed` state before every pre-existing test's `is_completed()` check could observe it; fixed via a direct `Iterator::all` fold over each animation's own `is_completed()`; verified via 2 new regression tests (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the full 136/136 scoped suite and clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-243 | [`WideOutlinePass::outline_pass` reads the final JFA result from the wrong ping-pong buffer -- inverted parity, always one step stale](./completed/243_wide_outline_jfa_final_buffer_selection_inverted.md) | Low | module/helper/renderer | 2026-08-17 | 2026-08-17 | `outline_pass` selected the buffer holding the converged JFA distance field from the parity of `num_passes` itself (`num_passes % 2 == 0` -> buffer 0), but the last JFA step actually run is `i = num_passes - 1` -- parities of `n` and `n - 1` are always opposite, so this picked the wrong buffer on every real invocation (`num_passes` hardcoded to 4), reading a one-step-stale distance field (thinner/less-converged outline, no panic); fixed by extracting the ping-pong parity rule into one shared `jfa_step_targets_fb0(i)` associated function both `jfa_step_pass` and `outline_pass` now defer to; verified via 2 new native unit tests (no GL context needed, following the BUG-180 precedent for this code area) plus the full 138/138 scoped suite and clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-244 | [`adjust_vibrance`'s saturation-push weight grows WITH existing saturation instead of against it -- boosts already-vivid colors (skin tones) harder than dull ones](./completed/244_color_grading_vibrance_inverted_saturation_weighting.md) | Low | module/helper/renderer | 2026-08-17 | 2026-08-17 | the shader's own doc comment documents vibrance as "smart saturation affecting less-saturated colors more," but the weight term `( mx - average )` INCREASES with a color's existing saturation (0 for gray, up to 2/3 for a fully saturated primary), so already-vivid colors (incl. skin tones) received a proportionally larger push than dull ones -- backwards from the contract; fixed by replacing the weight with the complement of a normalized HSV-style saturation, `( 1.0 - ( mx - mn ) / mx )`, decreasing with existing saturation as required; no GLSL ES execution path exists in this crate, so verified via a line-for-line Rust port (mirroring the BUG-178/`white_balance.rs` precedent) with 5 new native unit tests (2 confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the full 143/143 scoped suite and clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-245 | [glTF loader's material-variation cache is passed by shared reference and can never be written to -- every primitive sharing a material + vertex-defines combination gets its own independent clone instead of the intended shared instance](./completed/245_gltf_material_variation_cache_never_populated.md) | Medium | module/helper/renderer | 2026-08-17 | 2026-08-17 | `primitive_material_resolve` took `material_variation_map` by shared reference (`&FxHashMap`), so its cache-miss branch could never write the newly-created clone back into it -- every entry `materials_create` seeded as an empty `Vec::new()` stayed empty forever, and every primitive sharing a glTF material + vertex-defines combination got its own independent clone instead of sharing the intended `SharedMaterial` instance; fixed by extracting the lookup-or-insert pairing into its own `pub` function, `material_variation_resolve`, taking the map as `&mut` and inserting immediately after construction; verified via 4 new native unit tests against a minimal zero-GL-call `Material` stand-in (all 4 confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the full 147/147 scoped suite and clean clippy; confirmed via workspace-wide grep that no downstream consumer relies on `GLTF.materials`'s pre-fix length/ordering | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-252 | [`Skeleton`'s morph-target displacement-texture sizing formula collapses row width to `0` for primitives with few vertices relative to their attribute/target count, dividing by zero and permanently disabling the displacement-texture update](./completed/252_displacement_texture_size_zero_width_division_by_zero.md) | Medium | module/helper/renderer | 2026-08-17 | 2026-08-17 | the sizing formula's `.floor()` on the block-count ratio `sqrt(data_len)/vertex_displacement_len` can round all the way down to `0` when the vertex count is small relative to `attributes_count * targets_count`, and the resulting `a = 0` is then used as a divisor for the texture height, producing `+inf` that saturates to `u32::MAX` on cast -- silently caught by an unrelated `a.max(b) > max_size` check, masking the real cause and permanently disabling the primitive's morph-target update; fixed by extracting the formula into its own `pub` function, `displacement_texture_size_compute`, and adding `.max( 1.0 )` to the floored block count; verified via 3 new native unit tests (2 confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the full 62/62 scoped suite and clean clippy; filed as BUG-252 not BUG-246 after discovering `highest_id: 245` was stale against actual on-disk allocations up to 251 | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-253 | [`Camera::projection_matrix_set` accepts any matrix with zero validation, completely bypassing BUG-174's construction-time guards](./completed/253_camera_projection_matrix_set_bypasses_bug174_validation.md) | High | module/helper/renderer + examples/minwebgl/gltf_viewer | 2026-08-17 | 2026-08-17 | `Camera::new` (BUG-174) validates `aspect_ratio`/`fov`/`near`/`far` before calling `perspective_rh_gl`, but its sibling setter `projection_matrix_set` assigned its `gl::F32x4x4` argument straight to `self.projection_matrix` with zero validation, structurally bypassing every one of BUG-174's guards for any caller not going through `Camera::new` -- reachable via this crate's own bundled `gltf_viewer` example (canvas-resize handler recomputing the matrix directly, the only real call site workspace-wide); fixed by adding finite-component and invertibility checks to the setter itself, converting it to `Result< (), WebglError >`; verified via 3 new native unit tests (2 confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the full scoped suite and clean clippy; filed as BUG-253 not BUG-246 after discovering the concurrent session actor had already claimed TASK-246/247/248 and BUG-249/250/252 by filing time | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-255 | [`Lights::spot_push` accepts `inner_cone_angle == outer_cone_angle`, producing NaN in every fragment lit by that spot light](./completed/255_lights_spot_push_equal_cone_angles_nan.md) | High | module/helper/renderer | 2026-08-17 | 2026-08-17 | `shaders/main.wgsl`'s spot-light falloff computes `smoothstep( outer, inner, angle )`, dividing by `( inner - outer )` internally -- exactly `0.0` at the pre-fix doc comment's own documented `inner_cone_angle <= outer_cone_angle` boundary, producing NaN that propagates into every lit fragment; fixed by rejecting non-finite cone angles and tightening the invariant to strict `<` in `spot_push` itself, widening its existing `bool` "dropped" return semantics (zero call-site changes needed, confirmed via grep of all real call sites); verified via 4 new native unit tests (3 confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus clean clippy; filed as BUG-255 not BUG-254 after a fresh on-disk scan found 254 already claimed | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-256 | [`Mesh::clone`'s `skeleton` field aliases the original's `Rc<RefCell<Skeleton>>` instead of producing an independent clone](./completed/256_mesh_clone_skeleton_aliases_original_instead_of_cloning.md) | Medium | module/helper/renderer | 2026-08-17 | 2026-08-17 | `Mesh::clone`'s `skeleton` arm cloned the `Skeleton` value but discarded the clone back into the source's own `RefCell` instead of a new `Rc`, then returned the source's original `Rc` for the "cloned" `Mesh` too -- both ended up sharing one `Skeleton` allocation, unlike the correctly-independent `primitives` arm two lines above (and `Primitive::clone`'s identical pattern elsewhere in the crate), breaking `Node::tree_clone`'s documented "new independent scene graph subtree" contract for any of its 6 real call sites cloning a skinned/animated node; fixed by wrapping the cloned value in a fresh `Rc::new( RefCell::new( .. ) )`, mirroring `primitives`'s own pattern; verified via 1 new native unit test (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun, pre-fix panicking at the `!Rc::ptr_eq(..)` assertion) plus the full scoped suite (a first run hit an unrelated concurrent actor's in-flight `camera.rs` failure, a later re-run came back 71/71 passing) and clean clippy; filed as BUG-256 not BUG-255 after discovering the concurrent session actor had already claimed BUG-255 by filing time | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-257 | [WebGPU renderer's normal-matrix singular fallback packs the raw un-inverted block instead of identity, despite a comment claiming parity with `webgl::Node`'s BUG-171 fix](./completed/257_webgpu_normal_matrix_singular_fallback_diverges_from_webgl.md) | Medium | module/helper/renderer | 2026-08-17 | 2026-08-17 | `WebGpuRenderer`'s private `model_raw` fell back to the raw, un-inverted `rotation_scale` block on a singular world matrix's linear part, with a comment claiming this matched `webgl::Node::world_matrix_set`'s BUG-171 fix -- it didn't, BUG-171 falls back to identity, and the raw-block fallback scales normals by the object's own (possibly axis-collapsing) scale instead of leaving them unmodified, producing visibly wrong lighting for the same "flatten"/zero-scale-animation trigger BUG-171 itself names; fixed by extracting the computation into its own `pub fn normal_matrix_compute`, whose singular fallback now genuinely matches BUG-171's identity fallback (also making it independently unit-testable, matching the BUG-252 extraction precedent); verified via 2 new native unit tests (1 confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus clean clippy and `cargo check`; filed as BUG-257 after a fresh scan following the Group E fork's own BUG-256 filing found 257 free | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-258 | [`Renderer`'s per-material shader-program cache ignores a change in Image-Based Lighting availability once the material was already registered once](./completed/258_ibl_program_cache_ignores_ibl_availability_change_after_registration.md) | Medium | module/helper/renderer | 2026-08-17 | 2026-08-17 | `primitive_register`'s program cache only invalidated on a cache hit when the material's own `needs_recompile()` flag was set, never when the renderer-level `use_ibl` state baked into the same compiled program changed -- so a material registered before `Renderer::ibl_set` was ever called kept its IBL-less program bound forever regardless of how many times `ibl_set` was called afterward, contradicting `ibl_set`'s own doc comment promise; fixed by storing `use_ibl` alongside the cached program UUID and extracting the invalidation decision into a pure, unit-tested `program_needs_recompile` function; verified via 5 new native unit tests (2 confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus a clean `cargo check`; an earlier draft test file named `renderer.rs` was found to shadow the external `renderer` crate name and break the shared test binary's compilation for the whole module, renamed to `program_needs_recompile.rs` to resolve; filed as BUG-258 after a fresh on-disk scan found 257 as the highest existing ID | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-259 | [`SwapFramebuffer::new`'s doc comment claims it creates a depth/stencil renderbuffer that the function body has never created](./completed/259_swap_framebuffer_new_doc_comment_claims_nonexistent_renderbuffer.md) | Low | module/helper/renderer | 2026-08-17 | 2026-08-17 | `SwapFramebuffer::new`'s doc comment claimed it initializes "its WebGL framebuffer, renderbuffer, and the primary output texture" with the framebuffer "configured with a single color attachment point and a depth/stencil renderbuffer" -- the function body has never called `create_renderbuffer`/`renderbuffer_storage`/`framebuffer_renderbuffer` since the renderbuffer was deliberately removed from this struct in commit a54d680b ("Added bloom", 2025-05-28), and the doc comment (written earlier) was never revisited; no observable rendering defect today since every `Pass` implementation already disables `DEPTH_TEST`, but a forward-looking documentation trap; fixed by rewriting the doc comment to match actual behavior (comment-only change, no functional code touched); verified via a new `include_str!`-based source-text comparison test (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the full scoped suite and clean clippy; filed as BUG-259 not BUG-258 after a fresh on-disk scan found a concurrent sibling review agent had already claimed 258 for an unrelated IBL program-cache bug | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-260 | [`ibl::load`'s `mip_range` clamp is applied to `diffuse_texture` instead of `specular_1_texture`, the only IBL texture with a real mip chain](./completed/260_ibl_mip_range_applied_to_diffuse_instead_of_specular_1.md) | Medium | module/helper/renderer | 2026-08-17 | 2026-08-17 | `ibl::load`'s texture-parameter setup block bound 3 different textures to the single global `TEXTURE_CUBE_MAP`/`TEXTURE_2D` binding points in sequence, and the caller-supplied `mip_range` (`TEXTURE_BASE_LEVEL`/`TEXTURE_MAX_LEVEL`) was applied only after the *last* `TEXTURE_CUBE_MAP` rebind (to `diffuse_texture`, which has exactly one mip level) instead of staying adjacent to the *first* rebind (to `specular_1_texture`, the texture actually carrying the 10-level chain `IBL::num_mips` documents) -- latent today since 9 of 10 real call sites pass `None` and the 1 non-`None` caller passes a coincidentally-harmless `Some(0..0)`, but silently broken for any future caller passing a genuine range; fixed by extracting the filter/mip-range block into its own `pub fn ibl_texture_parameters_apply` with the `mip_range` application moved immediately after `specular_1_texture`'s own filter calls; verified via 1 new wasm32/browser unit test against a real headless-Firefox WebGL2 context (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-261 | [`Blender`'s `translation_blend`/`rotation_blend`/`scale_blend` zero out a node's transform whenever no blended `Sequencer` targets that channel](./completed/261_blender_zeroes_out_translation_rotation_scale_when_channel_absent.md) | High | module/helper/renderer | 2026-08-17 | 2026-08-17 | all three blend functions applied their accumulated result to the target node unconditionally, even when no weighted `Sequencer` carried that channel for the node -- overwriting the node's existing transform with the accumulator's `Default::default()` seed (`(0,0,0)` translation/scale, identity rotation) instead of leaving it untouched, unlike every sibling `AnimatableComposition` impl's (`Sequencer`/`Pose`/`Scaler`/`Transition`) established skip-if-absent convention; reachable on every `Blender::set` call for any glTF rig with a joint animated via only some of the 3 TRS channels (extremely common); fixed by adding an explicit `values.is_empty()` guard (or, for rotation, correcting the existing empty-case branch) before applying the accumulated result; verified via 1 new native unit test (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the combined scoped suite and clean clippy; filed as BUG-261 not BUG-258 after two concurrent session actors had already claimed BUG-258 and BUG-259 by filing time | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-262 | [`weights_sequence` panics on `[T]::chunks(0)` when a glTF mesh has morph-target animation channels but zero declared morph targets](./completed/262_gltf_weights_sequence_panics_on_zero_targets.md) | High | module/helper/renderer | 2026-08-17 | 2026-08-17 | `weights_sequence` computed `weights.chunks( components * targets )` with no guard against `targets == 0`, which `[T]::chunks` panics on unconditionally; a glTF mesh with a morph-target `weights` animation channel but no `mesh.weights` default array (a legitimate, spec-permitted combination) legitimately reaches this with `targets == 0`, crashing the entire glTF load; fixed by returning `None` immediately when `targets == 0`, letting every call site's existing `let Some(..) else { continue; }` skip path handle it gracefully instead of panicking; also promoted the function from module-private to `pub` so a regression test could call it directly; verified via 1 new native unit test (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun -- pre-fix panics with `chunk size must be non-zero`) plus the combined scoped suite and clean clippy; filed as BUG-262 not BUG-259 after a concurrent session actor independently claimed BUG-259 between this session's initial claim and file-write time | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-263 | [`CatalogBuilder::build()` double-reports a missing `(object, state)` pair when `state_require` is called twice with the identical pair](./completed/263_catalog_builder_duplicate_missing_state_double_reported.md) | Low | module/helper/tilemap_scene | 2026-08-17 | 2026-08-17 | the objects-accumulation loop in `CatalogBuilder::build()` dedupes missing ids via `seen_missing_objects`, but the parallel states-accumulation loop had no equivalent guard, so requiring the identical missing `(obj, state)` pair more than once pushed duplicate entries onto `CatalogError::missing_states`, inflating its `len()` and duplicating the corresponding `Display` line; fixed by adding a `seen_missing_states` set plus a `states.contains_key` guard, mirroring the objects loop's existing pattern; verified via 1 new native unit test (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the combined scoped suite (176 passed / 0 failed / 2 pre-existing-unrelated ignored) and clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-264 | [`vertex::canonicalize` reports the inverse rotation permutation, selecting the wrong `{rot}` sprite variant](./completed/264_vertex_canonicalize_rotation_reports_inverse_permutation.md) | Medium | module/helper/tilemap_scene | 2026-08-17 | 2026-08-17 | `canonicalize`'s `rotation` was computed as the inverse of the documented permutation -- searching where original corner 0 ended up in the sorted array, instead of reading out which original corner is now in canonical slot 0 (`indexed[0].0`) -- correct only at the `rotation == 0` fixed point, wrong for either non-identity cyclic rotation of 3 corners, silently selecting the wrong `{rot}`-substituted sprite variant in `vertex_pass_compile`; fixed by reading `indexed[ 0 ].0` directly; verified via 1 new native unit test (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus a combined scoped rerun confirming no regression on the pre-existing `canonicalize_sorts_ids` pin and clean clippy; filed as BUG-264 not BUG-263 after a fresh on-disk scan immediately before filing found a concurrent session actor had already claimed 263 | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-265 | [`Scene::from_snapshot` applies the snapshot's `seed` after spawning every instance, so `PhaseOffset::Instance` salts use the default seed instead of the declared one](./completed/265_scene_from_snapshot_seed_applied_after_spawn.md) | Medium | module/helper/tilemap_scene | 2026-08-17 | 2026-08-17 | `from_snapshot` spawned every tile/edge/multihex/free/viewport/entity instance before calling `seed_set(snap.seed)`, but `spawn` reads `self.seed` synchronously per-instance to derive `instance_phase_seed` -- so every snapshot-loaded instance was salted with the default seed (`0`) instead of the snapshot's declared one, silently defeating `PhaseOffset::Instance`'s re-seeding contract while `VariantSelection::Random` (which reads `scene.seed()` live at render time) stayed correct; fixed by moving the `seed_set` call to before the spawn loops; verified via 1 new native unit test (confirmed fail pre-fix / pass post-fix via temporary revert-and-rerun) plus the combined scoped suite and clean clippy; filed as BUG-265 not BUG-263/264 after two successive fresh on-disk scans found concurrent session actors had already claimed both | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-266 | [`GridRenderer::svg_grid_render`'s fallback arm calls itself instead of a square-grid helper, causing unconditional infinite recursion and a stack overflow](./completed/266_grid_renderer_svg_grid_render_infinite_self_recursion.md) | High | module/helper/tiles_tools | 2026-08-17 | 2026-08-17 | `svg_grid_render`'s wildcard `_` match arm (for `GridStyle::Triangular`/`Isometric`) called `self.svg_grid_render(writer, cell_size)` -- itself -- instead of a square-grid helper, since the square-grid body had never been factored out of the `Square4`/`Square8` arm into a callable unit; `self.style` never changes between calls, so the recursion is unconditional and deterministic, aborting the process (`SIGABRT`) on every call with either affected style; fixed by extracting the line-drawing body into `square_svg_grid_render` and calling it from both arms; verified via 1 new native unit test (confirmed fail pre-fix via isolated single-test run -- real stack overflow / SIGABRT / exit 101 -- and pass post-fix) plus the full scoped suite (all 10 test binaries + 40 doctests green) and clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-267 | [`direction_alignment_calculate`'s spurious `current_distance == 0.0` guard makes every ray-casting FOV hop converge onto the same neighbor regardless of direction](./completed/267_direction_alignment_calculate_spurious_current_distance_zero_guard.md) | High | module/helper/tiles_tools | 2026-08-17 | 2026-08-17 | `direction_alignment_calculate`'s zero-guard included `current_distance == 0.0`, but `current_distance` is never used as a divisor anywhere in the function (only `target_distance` is); since `directional_ray_cast` always starts each ray with `current = viewer.clone()`, the guard fired unconditionally on every ray's first hop for every candidate neighbor, forcing all of them to tie at alignment `0.0` and collapsing every ray onto the same first-iterated neighbor regardless of `direction_target`; fixed by removing the spurious `current_distance == 0.0` clause, keeping only the genuine `target_distance == 0.0` guard; verified via 1 new native unit test (confirmed fail pre-fix via combined `--no-fail-fast` run -- real panic, "expected immediate neighbor .. to be visible at range 1" -- and pass post-fix) plus the full scoped suite (197/197 in `integration_tests`, all binaries + 40 doctests green) and clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-268 | [`EventBus.statistics.total_subscribers` never decrements when a listener self-unsubscribes via `EventResult::Unsubscribe`](./completed/268_event_bus_total_subscribers_not_decremented_on_auto_unsubscribe.md) | Medium | module/helper/tiles_tools | 2026-08-17 | 2026-08-17 | `EventChannel::events_process`'s `-> ()` signature gave `EventBus` no way to learn how many listeners a self-unsubscribe (`EventResult::Unsubscribe`) removal had auto-removed from a channel, so `total_subscribers` was only ever decremented by the explicit `unsubscribe()` API path, silently drifting upward on every automatic removal while per-channel `subscriber_count` stayed correct; fixed by changing the signature to `-> usize` (propagated through the private `AnyEventChannel` trait) and applying the returned count via `saturating_sub` in both `events_process` and `events_for_type_process`; verified via 1 new native unit test (confirmed fail pre-fix via combined `--no-fail-fast` run -- real failure, exact expected assertion message -- and pass post-fix) plus the full scoped suite (13/13 in `events_test`, all binaries + 40 doctests green) and clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
| BUG-269 | [`GameStateSerializer::game_state_deserialize` never checks version compatibility -- `with_version` is a silent no-op and `IncompatibleVersion` can never be constructed](./completed/269_game_state_deserialize_never_checks_save_version_compatibility.md) | Medium | module/helper/tiles_tools | 2026-08-17 | 2026-08-17 | `game_state_deserialize` deserialized and returned the state unconditionally, never calling `SaveVersion::is_compatible_with` or constructing `SerializationError::IncompatibleVersion`, despite both being fully implemented and unit-tested in isolation -- `with_version()` was a silent no-op and the module's own documented "Version Management: Backward compatibility" feature never actually ran end to end; fixed by adding a compatibility check after deserialization, returning `IncompatibleVersion` on mismatch instead of `Ok(state)`; verified via 1 new native unit test (confirmed fail pre-fix via combined `--no-fail-fast` run -- real failure, exact `Ok(..)` payload shown -- and pass post-fix) plus the full scoped suite (10/10 in `serialization_test`, all binaries + 40 doctests green) and clean clippy | 0 | user1@w002/home/user1/pro/lib/yrd_gamedev/cgtools/task/bug/ (self) |
